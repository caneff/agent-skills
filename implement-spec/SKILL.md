---
name: implement-spec
description: "Parked: drive a sliced spec as one supervised run, workers on the ready frontier, one PR, a review loop until clean."
disable-model-invocation: true
---

> **Parked** until the multi-worker lane is rebuilt on the herdr lane — see
> [#700](https://github.com/caneff/agent-skills/issues/700)'s "Out of scope"
> note. Nothing below is executable policy until then.

A mixed-origin queue — several tickets from different specs, not one spec's
slices — is [`burndown`](../burndown/SKILL.md)'s job, not this skill's.

## Shape

Parked — the one-Run-per-spec task graph, dependency edges, and worker
dispatch described here belonged to the retired coordination tool.

## The brief

Parked — the file-ownership brief the worker used belonged to the retired
coordination tool.

## An unusable coordination tool is a stop

Parked — this section's stop condition belonged to the retired coordination
tool.

## Waiting

Parked — the wait primitive here belonged to the retired coordination tool.

## Gates

Parked — the gate mechanism here belonged to the retired coordination tool.

## Landing

Parked — the end-of-spec review loop and draft-PR step belonged to the
retired coordination tool; a single ticket runs through `implement/SKILL.md`
§ Finish instead.
