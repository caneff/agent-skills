# Merge tail: branches that conflict with each other

> **Parked** along with the rest of the burndown skill — see
> [#700](https://github.com/caneff/agent-skills/issues/700)'s "Out of scope"
> note. This was the linearize-and-force-push procedure for branches that
> conflict with each other after a burn's dry-run merge; it is retired with
> the tool it ran on and is not executable policy until the lane is rebuilt.
> Full text: `git show b390166:burndown/references/merge-tail.md`.
