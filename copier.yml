# Copier template for the Sandcastle orchestrator.
# `copier copy` installs it into a repo; `copier update` later merges template
# edits into an installed repo, keeping that repo's own additions.
#
# This file sits at the REPO ROOT on purpose. copier records `_commit` in a
# target's `.copier-answers.yml` only when its source is the git root; point it
# at a subdirectory instead and the breadcrumb pins no version, leaving
# `copier update` with no ref to diff from. So the config lives here and
# `_subdirectory` reaches down to the template tree.
#
# `_subdirectory` is `setup-sandcastle/templates`, NOT `.../templates/.sandcastle`
# (issue #93). The subproject root the template renders into is the TARGET's git
# root, with `.sandcastle/` a subtree beneath it and the answers breadcrumb at
# the root. That is what lets `copier update`, run from the target's git root,
# resolve its diff and do a real 3-way merge — the old `.sandcastle`-as-root
# layout put the breadcrumb in a subdir where root-level `copier update` could
# not find it. The dev-home harness (package.json, vitest.config.mjs, tests/)
# lives ABOVE this subdirectory in `setup-sandcastle/`, so it is structurally
# outside the render and never ships to a target.
_subdirectory: setup-sandcastle/templates

# The dev-only vitest suite guards the canonical `.mts`; a target never edits
# the orchestrator, so it never ships. `.sandcastle/tests/**` has never shipped,
# so there is nothing to withdraw — it stays excluded in place. Both patterns
# are needed: the bare path drops the directory entry, the `/**` drops its
# contents (a lone trailing-slash pattern leaves an empty tests/ behind). Paths
# are anchored at the subproject root (`templates/`), so the tests under
# `.sandcastle/` need the prefix.
#
# The project's own domain model — `CONTEXT.md` and the ADRs beneath it — is
# maintainer reading that no adopter runs, so it lives OUTSIDE the render tree
# entirely, at `setup-sandcastle/docs/`, not excluded from within it (issue
# #196). `_exclude` merely stops a file rendering; every adopter that already
# received it keeps it forever, and `copier update` carries no deletion. Moving
# the file out of `templates/` instead produces a real deletion in the render
# diff, which `copier update` then carries into every adopter for free.
_exclude:
  - ".sandcastle/tests"
  - ".sandcastle/tests/**"

# The template fills its own blanks with `[[ ]]` / `[% %]`, not copier's default
# `{{ }}` / `{% %}`. The prompt-drawer files under `.sandcastle/` carry their own
# `{{TASK_ID}}` / `{{BRANCH}}` placeholders, which the agent expands at RUN time —
# under the default delimiters copier would eat them the moment a prompt file
# becomes renderable. Distinct delimiters keep the two expansion passes apart.
# These apply to rendered path names too, hence `[[ _copier_conf.answers_file ]]`.
_envops:
  block_start_string: "[%"
  block_end_string: "%]"
  variable_start_string: "[["
  variable_end_string: "]]"

# The ecosystem the adopter is. `default: python` is load-bearing, not a
# courtesy: copier takes a defaulted answer silently under `--defaults`, so the
# existing Python adopters keep rendering correctly without anyone answering.
# Drop the default and copier raises `ValueError: Question "LANGUAGE" is
# required` instead, aborting the update — the first propagate sweep would skip
# every adopter it reached.
LANGUAGE:
  type: str
  help: "The adopter's ecosystem — decides the sandbox image and the build commands"
  choices:
    - python
    - node
  default: python

# Asked only of a Python adopter. copier drops an unasked conditional answer
# from the breadcrumb, so a Node adopter's recorded version disappears on its
# own rather than lingering for the propagate sweep to read back and re-assert.
PYTHON_VERSION:
  type: str
  help: "Python the sandbox bakes in — the target's .python-version (e.g. 3.14)"
  default: "3.13"
  when: "[[ LANGUAGE == 'python' ]]"

# The commands below are DERIVED from LANGUAGE, never asked: `when: false` keeps
# each out of the prompt sequence and out of the breadcrumb, so an adopter can
# neither drift from its ecosystem's commands nor be re-asked about them on a
# sweep. Changing a command is a template edit that reaches every adopter.
#
# The Node arm calls npm scripts because that is what `setup-sandcastle` asserts
# a Node adopter defines (lint, typecheck, test) — there is no `check` script to
# lean on, so the gate composes the three the way `just check` composes them.

CHECK_COMMAND:
  type: str
  when: false
  default: "[% if LANGUAGE == 'node' %]npm run lint && npm run typecheck && npm run test[% else %]just check[% endif %]"

LINT_COMMAND:
  type: str
  when: false
  default: "[% if LANGUAGE == 'node' %]npm run lint[% else %]just lint[% endif %]"

TYPECHECK_COMMAND:
  type: str
  when: false
  default: "[% if LANGUAGE == 'node' %]npm run typecheck[% else %]just typecheck[% endif %]"

# `npx vitest run <files>`, NOT `npm run test <files>`: npm needs a `--`
# separator to forward arguments to the script and silently drops them without
# it, which would quietly run the WHOLE suite as the per-commit "scoped" check.
SCOPED_TEST_COMMAND:
  type: str
  when: false
  default: "[% if LANGUAGE == 'node' %]npx vitest run <files>[% else %]uv run pytest <files>[% endif %]"
