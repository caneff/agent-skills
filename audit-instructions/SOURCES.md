# Sources

Verified 2026-07-31. Host is `platform.claude.com` — `docs.claude.com` 302s here.

Fetch both:

- https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices
- https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5

The second is the model-specific page — swap it for the model running this session:
`.../prompt-engineering/prompting-claude-<model>` (`prompting-claude-fable-5`,
`prompting-claude-sonnet-5`, `prompting-claude-opus-4-8`, …).

`claude-prompting-best-practices` is the living reference and absorbed the old
per-technique pages (`be-clear-and-direct`, `multishot-prompting`,
`claude-4-best-practices`, `extended-thinking-tips`) — they all redirect into it now.
Fetching it once covers every technique.

A dead URL is a **NOT RUN**, and the fix is one edit here.
