# Spec #1024 close-out: the seam test, two opens, the spec-level review

Closing ticket #1096 for spec #1024 (review leftovers: fix small findings
in-round, one sweep ticket per burn). The end-to-end test is
`tests/review_leftovers_e2e_test.py`; its docstring states what the seam is
blind to.

## One open of the real thing: the sweep ticket

**Checked on #1097**, "Sweep: leftovers from burn spec-1024", this spec's own
run sweep: `sweep.py render` over the `spec-1024` run file, filed through
`/file-ticket`.

- Read its live body with `frontier.blocked_by_section`: one declaration,
  `- None — can start immediately.`; the body contains `## Blocked by`
  exactly once.
- Grouped by file (`## burndown/runfile.py`, `## burndown/sweep.py`, …), one
  bullet per item carrying id, severity, title, clump, ticket, PR and one line
  of finding text.
- A sweep worker acted on it without reopening a PR thread: #1111
  (8c56a81) closed #1097 with one commit per item.
- One deviation from the renderer's raw stdout: the filed body opens with a
  controller-written paragraph instead of the render's title line (spec review
  P5 below).

## One open of the real thing: an adjacent fix, measured

**Checked on PR #1102** (ticket #1049, `loop.py picks`), a live review round
after #1077 landed the rule.

- Round-1 standards finding S1 (judgement, `burndown/loop.py`, the `picks`
  docstring) was fixed in the round rather than filed: PR body line
  `S1 (standards, judgement): fixed (adjacent), 2ec9309`.
- The sidecar `~/.cache/agent-reviews/skills/dispositions-1049.jsonl` carries
  `{"id":"S1","outcome":"fixed","sha":"2ec9309","scope":"adjacent"}`.
- The verification report `review-verify-1049.md` ran
  `check_adjacent.py --base origin/main`, which printed
  `S1: ok, 5 changed lines in burndown/loop.py` and exited 0.
- Seven more rounds since then recorded adjacent fixes in their sidecars
  (#1000, #1040, #1044, #1056, #1085, #1087, #1097); only #1049's report was
  read end to end for this note.

## The spec-level review

`/multi-axis-code-review 97f699f~1` over a detached worktree holding the six
squash commits cherry-picked in landing order (all applied clean; 25 files,
+2090/−73). Reports and sidecars:
`~/.cache/agent-reviews/skills/{review,findings}-{standards,spec,correctness}-spec-1024.*`.
Severity per `implement/SKILL.md` § Review: only correctness `CONFIRMED` is
high here. #1111 (8c56a81) landed after the spec and before this review's
base, so findings it already fixed are marked with that sha.

| Id | Finding | Disposition |
|----|---------|-------------|
| S1 | `sweep.py` counts skipped malformed sidecar lines | fixed, 8c56a81 (#1111) |
| S2 | outcome tuple and leftover key list duplicated | leftover — the tuple half is fixed in 8c56a81; `read_leftover_lines` still spells the key list out |
| S3 | stale pointers: `run-file.md` "a later ticket (#1030, #1033)"; `render_body` docstring points at run-file.md for the zero-leftovers rule | leftover |
| S4 | § The merge step 3's "records the head sha" has no place or command | leftover |
| S5 | whole-clause needles in five wording tests | leftover |
| P1 | Codex leftovers on a direct `/implement` never reach the per-PR sweep | filed, #1125 |
| P2 | gate-pass sentence still offers `filed` and no `leftover` | leftover |
| P3 | `counts` misses Codex-pass fixes and filings | leftover |
| P4 | disputed dispositions go to the verifier as claims | disputed — the owner's standing rule on verification briefs, not scope creep |
| P5 | `sweep.py render` stdout opens with the title; the skill says to file its stdout | leftover |
| P6 | same as S1 | fixed, 8c56a81 (#1111) |
| P7 | same as S3 | disputed — duplicate of S3, disposed there |
| C1 | `runfile.py leftover` drops a second leftover sharing an id (CONFIRMED, reproduced on 93a05de) | filed, #1124 |
| C2 | same gap as P1 (CONFIRMED) | filed, #1125 |
| C3 | "no run file under it" has no signal the worker can read | leftover |
| C4 | same as S1 (CONFIRMED on the reviewed commits) | fixed, 8c56a81 (#1111) |
| C5 | `sweep.py counts` keys the review cache on cwd | filed, #1093 (already open) |
| C6 | `frontier_test.py` fold witness asserts only "not AMBIGUOUS" | leftover |
| C7 | the sweep update path does not restore `## Blocked by` | leftover |
