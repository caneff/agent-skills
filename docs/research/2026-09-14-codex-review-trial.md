# Codex adversarial-review trial (#812)

Chris ruled a trial, not a permanent rule (2026-09-14): does a Codex
adversarial pass find real problems the three Claude review axes miss? Chris
ruled again (2026-09-15, #817) to move the pass off the worker it
reviews — independence needs the builder out of the loop — to the
controller, at merge time. The procedure the controller follows lives in
`implement/SKILL.md` § The merge step 3; this note doesn't restate it.

One row is appended here per heavy Claude-lane ticket that ran the pass (a
skipped pass adds no row), as an auto-ship commit on `main` after the
merge, counted as rows land rather than as PRs are drafted. The trial ends
after five rows: the controller whose row brings the count to five brings
Chris this table plus a keep/drop recommendation — keep if at least one
`codex-only, confirmed` finding would have shipped a real bug, drop if the
pass only repeated the Claude axes or raised noise.

The #814 row was classified by the builder (pre-#817 rule); from #817 on, rows are classified by the controller against the PR body's full round-1 list.

**Last reviewed: 2026-09-20** (through the `agent-skills#869 + #946` row). The
five-row trigger above counts from this line, not from the top of the table:
the controller whose row is the fifth after the date here brings Chris the
table and the keep/drop recommendation, then moves the date to its own row.
Added because the trigger had become uncomputable — 44 rows had landed with
no record of which had already been read.

| Ticket | PR | codex-only, confirmed | also found by Claude | disputed | codex-only confirmed findings |
|---|---|---|---|---|---|
| #814 | #816 | 1 | 2 | 1 | Documented worker invocation interpolated the ticket body directly into a double-quoted shell string (`"<ticket body verbatim>"`), so a body containing `"`, backticks, or `$(` would run as shell instead of reading as text. |
| #817 | #818 | 3 | 0 | 0 | Run 1 [high]: the PR body listed only disputed/filed round-1 findings, so a fixed Claude finding vanished and a matching Codex finding would be miscounted as codex-only. Run 1 [medium]: the controller invocation printed to stdout with no file bound, so no raw-output record existed to post. Run 2 [high]: the pass left its output and ticket-body files in `.scratch/`, so `merge-cleanup` (which refuses ignored `.scratch/` content without `--discard`) would stall after every pass. |
| #822 | #827 | 1 | 1 | 1 | Run 2 [high]: the `PRE_REPORT_KEEP_SCRATCH` escape hatch passes the gate but no PR-up field or controller check carries its reason, so kept `.scratch/` surfaces only when `merge-cleanup` refuses (filed #831; fail-safe, a visibility gap rather than a shipped bug). Also found by Claude: run 1's unreadable-`.scratch/` false pass, raised by the Correctness axis and disputed by the worker, fixed after Codex raised it. Disputed: run 2's still-running-writer refill (cleanup fails closed). |
| #821 | #829 | 3 | 0 | 0 | Run 1 [high]: the claim-clearing `gh issue edit` result was discarded, so cleanup reported success after a failed edit with the branch already gone (fixed, re-run command printed). Run 1 [medium]: `--remove-assignee @me` cleared only the runner, not the issue's real assignees (fixed; in this lane dispatch claims as the same login, so it bites only on a reassigned ticket). Run 2 [high]: that failure path still exits 0 (filed into #823). |
| #819 | #830 | 2 | 0 | 0 | Run 1 [high]: the session lookup took the first live registry record in the worktree by filename order, not the one matching the dispatched herdr agent's `agent_session.value`, so a second session in the worktree printed the wrong name (fixed, sessionId match plus a two-session witness test). Run 2 [medium]: the lookup gives up after ~100 ms, so a late SessionStart hook prints `(unavailable)` under load (filed #836). |
| twitch-rules-scroller#295 | #298 | 4 | 0 | 0 | Run 1 [high]: `CurrentMode()` turned an `EnumDisplaySettings` failure into literal text and `ResSet` wrote that string as the recovery record before changing the display, so a transient query failure left `res-state.txt` holding `(could not read the current mode)` while the registry default was already overwritten — restore then refuses and the operator has no original mode to return to (fixed: fallible `TryCurrentMode`, abort before any write or display change). Run 1 [high]: no lock covering state read / mode apply / delete, so a concurrent invocation could leave a mode active with no recovery record — concrete once #290's hotkey can double-fire (fixed: non-blocking `Global\stream-rig-res` mutex). Run 2 [high]: `DISP_CHANGE_RESTART` deletes the recovery record although Windows has already persisted the requested mode, so the display switches at next reboot with restore refusing (filed #299). Run 2 [medium]: state and both `ChangeDisplaySettingsEx` calls carry only width/height, so restore cannot guarantee the original refresh rate — measured not to bite on the target 359 Hz display, but unguaranteed (filed #300). |
| sudokumaker-custom-constraints#351 | #499 | 0 | 0 | 0 | Run 1: approve, no material findings (round 1: S1–S4 fixed in ff5bb5f; P1, C1, C2 disputed). |
| sudokumaker-custom-constraints#401 | #500 | 1 | 0 | 0 | Run 1 [high]: the known-grid acceptance guard, the only check against an over-constrained encoding wrongly reporting UNIQUE, sat outside `check`, `check-full` and CI, so a regression could merge green (fixed in a779c9e: a shape-covering 6-grid `--cover` subset in `just check`, ~7 s CI; the sibling probe's same gap filed #501). Run 2: approve. |
| sudokumaker-custom-constraints#402 | #502 | 1 | 0 | 0 | Run 1 [high]: the branch closed the uniqueness ticket, but its only real run (cand_00, 12 circles) stopped NOT PROVED at the 20-min cap (928 survivors, 0 fills), so completion could be misread as a proof (filed #503; PR body and note state NOT PROVED; the controller ruled the ticket asked for the method, not the cand_00 verdict). Run 2: approve. |
| sudokumaker-custom-constraints#355 (clump: #357 #360 #361 #362 #363 #364) | #504 | 1 | 0 | 0 | Run 1 [medium]: `link_swap.swap_main` dispatched to rebuild with no `--component` after refusing only `--board`, so `build_link.py --backend candidate.js` on bare-board/running-start silently wrote a normal rebuild with the checked-in backend and reported success, which could overwrite a link or fake a backend-change timing artifact (fixed in 24e235b: `rebuild_reads_backend`, only house-gac declares it; CLI tests for the refusals). Run 2: approve. |
| sudokumaker-custom-constraints#358 | #505 | 0 | 0 | 0 | Run 1: approve, no material findings (round 1: C1 fixed in b143f53; P1–P3, C2 and others disputed in the PR body). |
| twitch-rules-scroller#275 | #304 | 3 | 0 | 0 | Run 1 [medium]: the collection referenced three untracked, generated frame PNGs after their frames were unbaked from the background, so a reload succeeded even when rendering was skipped or failed and showed frameless stickers — the exact state Chris had already hit live by a different route that day (fixed: fail-closed asset preflight in both `install-collection.sh` and the server's reinstall). Run 2 [high]: `start()` fires the asset preflight before sticker regeneration finishes, so a restart missing a new frame PNG stays on the old collection with no retry (filed twitch-rules-scroller#305). Run 2 [medium]: the docs asserted "no jagged edge" from an offline Pillow diff, which cannot establish OBS's rendering of a tilted RGBA edge, and gave Chris no pass/fail instruction for the #276 verdict (fixed by the controller as a doc auto-ship) — the verdict then came back fail, confirming the finding. |
| twitch-rules-scroller#306 | #308 | 1 | 0 | 0 | Run 1 [high]: re-raised the twitch-rules-scroller#305 startup race against this PR's newly required combined frame PNG (filed: #305, already open; the asset was already live on the target machine, so this deploy could not hit it). Run 2: approve, no findings. |
| sudokumaker-custom-constraints#484 | #507 | 3 | 0 | 1 | Run 1 [high]: the fresh-hunt refusal checked for existing output and then created it non-exclusively, so two hunts on the same absent `--out` could both proceed and interleave `examples.jsonl` with cross-process duplicates and a one-sided `summary.json` (fixed in e2342d8: `O_CREAT\|O_EXCL` claim on `run.json`, two-subprocess race test). Run 1 [medium]: custom symmetry cell maps were unvalidated, so a non-bijective map collapsed unrelated grids and silently dropped verified candidates as duplicates (fixed in e2342d8: permutation check). Run 2 [high]: the fix checked permutations but not group closure, so `[identity, rotate90]` passes and still falsely merges grids (filed #508). Disputed: run 2's interrupted-hunt recovery (scope of #487, added there as an acceptance test). |
| sudokumaker-custom-constraints#485 | #510 | 0 | 0 | 0 | Run 1: approve, no material findings (round 1: 6 fixed, 3 disputed in the PR body). |
| sudokumaker-custom-constraints#508 | #511 | 0 | 1 | 0 | Also found by Claude: run 1 [high] a closed custom group whose map length differs from `key()` passes preflight and crashes after output exists (round-1 P1/C3, filed #509 before the pass). No run 2: no fix commit followed. |
| twitch-rules-scroller#301 | #309 | 3 | 0 | 0 | Run 1 [high]: the display ID came from a Windows env var, but OBS launches via WMI (`launchDetached`) with no environment block, so the rig-spawned server need not inherit a `setx` value — the worker's own inheritance check tested the harness-side process, not the one that applies the ID, and the escape-hatch scene would have stayed blank after every restart, the bug the ticket exists to fix (fixed: deployment-local file under `%LOCALAPPDATA%\stream-rig\`, e2e now asserts what the server resolved). Run 2 [high]: the e2e check compares ID strings, and `SetInputSettings` stores and reads back any string, so an invalid or double-escaped device path passes with no display selected (filed twitch-rules-scroller#311). Run 2 [medium]: `/api/obs` publishes monitor success before OBS applies it (filed in #311). |
| sudokumaker-custom-constraints#486 | #512 | 2 | 0 | 0 | Run 1 [medium]: the solve watchdog armed its stall clock only after the first fixed 1 s join, so a zero-solution solve with a sub-second stall limit ran ~2 s before `StopSearch` (fixed in c450b27: clock armed before the solve thread starts, poll at `min(1.0, stall)`, real-clock regression test). Run 2 [high]: `check_uniqueness` adds its exclusion constraint to the caller's model, so a second call on a two-solution model returns `unique` — a stripper reusing a base model would certify invalid clue removals (filed #514). |
| twitch-rules-scroller#307 | #312 | 1 | 0 | 0 | Run 1 [medium]: `server.scene-watch.test.mjs` still drove the real `watchObsScenes` → `reinstallCollection` path against the checked-in collection and the live art folder, violating the ticket's own "no test reads the live folder" criterion — the second such file found after the worker believed the set complete, and it had only been "passing" because a missing mock made the reinstall throw early (fixed: temp fixture plus a full enumeration of every test that reaches the path, witnessed with a collection naming an absent PNG). Run 2: approve, no findings. |
| sudokumaker-custom-constraints#514 | #515 | 0 | 0 | 0 | Run 1: approve, no material findings (round 1: S1, S2, P1, C1 fixed in 3148642). |
| sudokumaker-custom-constraints#487 | #513 | 4 | 0 | 1 | Run 1 [high]: fresh-vs-resume was classified before taking `.lock`, so a second process could act on a stale "no run.json" read after the first hunt completed and overwrite it (fixed in 62ce4d2, thread-barrier test). Run 1 [high]: `seed_done` flushed before the state save, so a kill in between skipped the seed on resume with stale finder state (fixed in 62ce4d2: state records its seed, reconciliation trims progress ahead of state). Run 1 [medium]: dedupe rebuilt from `progress.jsonl`, not `examples.jsonl` as specified (fixed in 62ce4d2). Run 2 [high]: negative `--seeds` collide with the `-1` no-state sentinel, losing a killed seed's state silently (filed #516). Disputed: run 2's rerun of a seed killed mid-checkpoint (uncommitted seed; at-least-once recovery). |
| sudokumaker-custom-constraints#516 | #518 | 1 | 0 | 1 | Run 1 [high]: the hunt CLI still rejected the two-token `--seeds -3:-2` on Python 3.11–3.13 (argparse reads it as an option); the tests only passed because the harness used `--seeds=-3:-2` (fixed in b3422c0: token merge before parsing, subprocess test red on 3.12). Disputed: run 2's cross-spelling resume refusal (fails closed, `run.json` holds the argv). |
| twitch-rules-scroller#305 | #313 | 3 | 0 | 0 | Run 1 [high]: the bounded render wait turned a render that succeeds after `STICKER_RENDER_TIMEOUT_MS` into a permanent refusal with no retry, leaving OBS on the old collection — the #305 end state by the timeout path (fixed in 6fe65ab: one-shot late-success reinstall retry, test). Run 1 [high]: the boot render writes image-source PNGs in place, so a render that fails after writing the frameless background visibly changes the retained old collection (filed into twitch-rules-scroller#303, scope comment added by the controller). Run 2 [high]: the late retry's own Scratch-and-back switch bumps `collectionChangeSeq` on the persistent watcher, so the retry never records `collectionInstalled` and readiness stays false though the collection loaded (filed twitch-rules-scroller#314). |
| sudokumaker-custom-constraints#509 | #521 | 2 | 0 | 0 | Run 1 [high]: a resume refused on a symmetry/key-length mismatch had already reconciled and rewritten the prior hunt's logs and summary, contradicting "left exactly as found" (fixed in 11b4ecb: snapshot before resume, restore on mismatch). Run 2 [high]: that restore uses non-atomic `write_bytes()`, so a kill mid-restore can corrupt the prior hunt (filed #525: check before mutating, drop snapshot/restore). |
| sudokumaker-custom-constraints#488 | #520 | 2 | 0 | 0 | Run 1 [high]: `HUNT_FAKE_LOAD1=nan` bypassed the load gate (`nan > 24` is false), and the variable leaks into real hunts through inherited environments (fixed in dbf9742: finite, non-negative only). Run 2 [medium]: hunt subprocess tests read the real load, so `just test` fails on a box above load 24 (filed #526). |
| sudokumaker-custom-constraints#489 | #523 | 3 | 0 | 0 | Run 1 [high]: `hunt verify` took no directory lock, so it could snapshot a still-growing `examples.jsonl` or race another verify on a shared temp file (fixed in 2a6f6ba: same flock as `run`, deterministic held-lock test). Run 2 [high]: `--no-verify` is not in the resume identity, so a resume silently switches verification mode; and a failed re-verify replaces a complete `verified.jsonl` with a prefix (both filed #527). Run 2 [high]: verify-then-resume leaves `verified.jsonl` stale (filed into #517). |
| sudokumaker-custom-constraints#490 | #524 | 3 | 0 | 0 | Run 1 [medium]: a swallowed render failure still marked the seed done, so resume never retried and the hunt ended with an example lacking its PNG (fixed in d0f216b: resume repairs missing PNGs for stateless finders). Run 2 [high]: PNGs are saved non-atomically, so a failed write leaves a partial file that repair treats as complete (filed into #522). Run 2 [medium]: stateful finders never get render repair (filed into #522). |
| sudokumaker-custom-constraints#526 | #528 | 0 | 0 | 0 | Run 1: approve, no material findings (round 1: S1/P1/C1, S2, S3/C2 fixed in 06f4c76; V1 disputed). |
| sudokumaker-custom-constraints#525 | #530 | 2 | 0 | 1 | Run 1 [high]: the pre-resume symmetry probe skipped done seeds, so a fully-done resume never validated the group and still rewrote files on a changed key shape (fixed in 2215902: check against persisted `__dedupe_key__` lengths). Run 1 [medium]: the probe snapshotted `save_state()` by reference, so an in-place `propose()` mutation survived the restore (fixed in 2215902: JSON deep copy). Disputed: run 2's later-seed key-shape change (a finder whose key length varies breaks the contract). |
| agent-skills#870 | #872 | 0 | 0 | 0 | Run 1: approve, no material findings (round 1: S2 fixed; S1, C1, C2, C4 disputed; C3 filed #871). |
| sudokumaker-custom-constraints#527 | #536 | 0 | 0 | 0 | Run 1: approve, no material findings — resume identity now includes the verification mode, and a failed deferred re-verify leaves the prior published verdict file untouched. |
| sudokumaker-custom-constraints#519 | #540 | 0 | 0 | 0 | Run 1: approve, no material findings — the lock inode survives cleanup, so a second run can't lock a replacement path before this run releases its own (round 1: S1/C1, S2 fixed; S3 partial/non-blocking; P1 and P2/C2 disputed). |
| sudokumaker-custom-constraints#522 | #539 | 0 | 1 | 1 | Runs 1 and 2 [high], same finding both passes: `_reconcile_renders` skips any finder with `load_state`, so the orphan-PNG sequence still holds for stateful finders and the new test pins it. Disposed as ship-with-scope-stated: the carve-out is deliberate (round-1 C1 — eager deletion bets an intact picture against a rerun with no repair path), and Codex's own precondition for closing it is #538, filed before the PR opened and blocked by it. Its closing ask (an end-to-end renders-vs-examples assertion for every finder type) was recorded onto #538. Two no-ship verdicts overridden by the controller. |
| twitch-rules-scroller#318 | #320 | 0 | 1 | 0 | Run 1 [high]: the new `wsl.exe` render child ran with `stdio: "ignore"`, so a failed render discarded its stderr and `regenerateStickers` logged neither exit code nor signal — the fail-closed preflight would then refuse the #221 reinstall all session with the repair target invisible, the exact shape of the incident #318 was filed from. Also raised by the worker's own Correctness axis (C2) and filed as #319 before the pass; the controller pushed for it to ship with the fix rather than be deferred, and it was (a1154e5: piped stderr, exit code, signal, cwd and the last 2 KB logged, two tests; #319 now closes with the PR). Run 2: approve, no findings. |
| sudokumaker-custom-constraints#456 | #553 | 0 | 0 | 0 | Run 1: approve, no material findings — the narrowed affected set and the `validate` guard are aligned, both mains pass the computed window, malformed local bent groups no longer half-register, and the raw-mask calls match the bundle API (round 1: C2, S3, S4, P4 filed as #552; S1, P5, S5 disputed in the PR body). |
| sudokumaker-custom-constraints#453 | #554 | 1 | 0 | 0 | Run 1 [medium]: the cut-starve `allowed` row was kept — the worker found the ticket premise false (`domTree`/`cutFilter` read the row) and disputed removal as P1, and the controller verified the read sites and ruled in its favour. Codex supplied the third option neither had considered: not remove-and-change-behaviour, but remove and derive allowedness from the live candidate mask (fixed in 7db5351: `cellAllows`, parent scan drops its `allowed` check since `dist[p] === dist[v] - 1` implies it; floor 0 weaker, cold 0.83 to 0.79). Run 2: approve, no findings. |
| sudokumaker-custom-constraints#454 | #555 | 0 | 0 | 0 | Run 1: approve, no material findings — the pooled matching/SCC buffers preserve their capacity bounds, and the raw-mask and plural-change calls match the documented bundle API (round 1: S1, S2, P1, C1, C2 fixed; S3 and S4 disputed in the PR body). |
| sudokumaker-custom-constraints#501 | #557 | 0 | 0 | 0 | Run 1: approve, no material findings — the inverted known-grid guard now runs in `test`, so `just check`, `just check-full` and PR CI all cover it, and the subset with its ~25s cost is documented (round 1: P1, S3, C1 fixed; S1 filed #556; S2, C2, C3 disputed). |
| sudokumaker-custom-constraints#547 | #558 | 0 | 0 | 0 | Run 1: approve, no material findings — both mains reject non-square boards before registration and throw on a missing coordinate before `| 0` can coerce it to cell 0, and the new regression test is picked up by the shared-test glob (round 1: S1, S2, P1, C1, C2 all disposed in the PR body). |
| sudokumaker-custom-constraints#559 | #561 | 0 | 1 | 0 | Run 1 [medium]: worktrees created before this change keep the artifact at the old `.scratch/ghosts/build/` path, so `merge-cleanup` still refuses them — true, and already filed by the worker as P2/C4 on #560 before the pass ran, hence also-found-by-Claude. Disposed as filed. Its recommended remedy (keep `build/` and commit `build/.gitignore` with `*`) was refused: that is the approach 5d2035c tried and the dry run disproved, since `build` is not in `CACHE_DIRS` — Codex had made the same misreading of `merge-cleanup --help` that produced the wrong ticket in the first place (agent-skills#875). No second run: the disposition was `filed`, no fixes were pushed, and the diff Codex would re-read was byte-identical at 203ac7a. |
| sudokumaker-custom-constraints#498 | #562 | 0 | 0 | 0 | Run 1: approve, no material findings — the added witnesses are in both the full known-grid guard and the CI cover subset, and the repo's own feasibility evidence establishes 1x8/1x9 and circled size 5 as impossible, so their absence is not a coverage gap (round 1: 7 fixed, 3 disputed). Codex independently confirmed the ticket-premise substitution the controller had ruled on. |
| sudokumaker-custom-constraints#455 | #551 | 0 | 0 | 0 | Run 1: approve, no material findings — the empty-line guard precedes the vacuous gate and DP, the raw-mask removals match the documented API, and the per-length scratch buffers stay bounded and are cleared before reuse (round 1 disposed in the PR body). Reviewed after a rebase onto the #451 collision; CI on the merge ref also cleared the #564 harness-lib mock rework. |
| agent-skills#876 | #878 | 2 | 0 | 2 | Run 1 [high]: `reap_one` handed `cleanup_branch` the branch name alone, so the branch was re-resolved at the destructive call and the plan's path filter applied only at enumeration — the worker reproduced it before fixing (a pre-push hook fired by the first workspace's own cleanup moved the second branch to a worktree outside `.claude/worktrees/`, and the pre-fix run removed it), then fixed in 5a8f18a by re-reading the holder immediately before removal and refusing a workspace that moved. Run 2 [high]: that same re-check uses `linked_worktree_holding`, which returns `None` when the primary checkout holds the branch, so a branch moved there between plan and removal passes a check written to prove the planned workspace still holds it (filed #881 by the controller — no third run, and the outcome is narrower than `high`: every candidate is already merged, the tip is recorded under `refs/deleted/`, and `cleanup_branch`'s own removal branch is gated by `linked_worktree_holding` too, so a primary checkout is never a removal target). Disputed: run 1's `git worktree remove --force` (git's own flag inside the shared single-branch path #876 puts out of scope; its narrow guard-to-removal-window point filed #879) and run 1's symlink escape (fixed anyway in 5a8f18a, but git resolves a worktree's path at registration, so the hole was not reachable — the boundary test says so in as many words). |
| agent-skills#877 | #882 | 0 | 1 | 0 | Run 1 [high]: `implement/codex-lane.md` still hands its builder the ticket body "verbatim" and reuses a body-only `body_file` for its own adversarial review, so a `--codex` build misses comment-added requirements — the exact failure #877 exists to close, on the one path #877's tests do not guard. True, and already raised by the worker's own Priority axis (P1) and filed as #880 before the pass ran, hence also-found-by-Claude. Disposed as filed by the controller. No second run: the disposition was `filed`, no commits followed, and the diff Codex would re-read was byte-identical at `b96aa32` — the second occurrence of step 3 mandating a run whose input cannot have changed (the first was #559), now going into the #782 spec rather than being re-litigated per PR. |
| agent-skills#889 | #904 | 5 | 0 | 0 | Run 1 [high]: dispatch sent an all-`documentation` clump to light tier, which lands with no PR, and cleanup's only clump source is the merged PR's `closingIssuesReferences` — so a light clump cleared one ticket and left every sibling claimed, the exact state #889 exists to end, while the acceptance criterion stayed vacuously true (controller ruled a clump is always heavy; fixed in `ab1f37e`, witness `a_clump_is_always_heavy_even_when_every_ticket_is_documentation`). Run 1 [high]: `pr_closed_tickets` collapsed a failed `gh` call and a non-JSON answer into the empty vector, so cleanup cleared the branch ticket, printed success and left the clump claimed; the code comment defended this by saying `is_merged` reports the transient, which is circular — when the list comes back empty there are no sibling tickets whose read would report it, and the parse arm was never transient (fixed in `ab1f37e`: `Result`, run fails naming the read to redo). Run 2 [high]: `is_merged` resolves the exact PR by `headRefOid == tip` and discarded the number into a display string, while `pr_closed_tickets` re-queried `--head <branch>` and unioned every merged PR that name ever carried, so cleanup could unclaim an older landing's tickets (fixed in `3f13c90`: `Merged` carries `pr`, read with `gh pr view`; it must be carried, not re-derived, because by step 7 the branch is deleted and the tip cannot be matched again; witness lists the older PR first to catch a first-match implementation). Run 2 [medium]: valid JSON of the wrong shape still became an empty successful list — the same class as run 1's finding one layer down, contradicting the guarantee `ab1f37e` had just established (fixed in `3f13c90`). Run 2 [high]: the clump claim reads every ticket then issues unconditional `gh issue edit` calls, so all-or-nothing holds only against its own snapshot (filed #905 by the controller, not built: Codex's primary remedy — a tracker-side conditional claim — does not exist, GitHub has no compare-and-swap on issue labels, and its fallback per-repository lock is new machinery outside this ticket; exposure is bounded by one dispatcher at a time, recorded on #905 as an assumption rather than left as silence). No third run: the two-run ceiling. Controller verified 203 green in `flow/lane` and read the post-review delta before merging.
| agent-skills#886 | #902 | 0 | 2 | 2 | Both run-1 findings restated trade-offs the worker had already reasoned through and disposed, and both recommended the direction its own measurements had disproved. Run 1 [high]: an active background shell or Monitor task drops out of the waiting set when an inbound message starts a new turn, so that stop alerts while the job runs — true, and it is the worker's own round-2 finding V1, taken deliberately. Codex recommended tracking liveness "using a reliable task-status source"; no such source exists in the transcript (965 of 1985 launches across 252 transcripts never emit a terminal notification, and the lifetime distributions of those that do and do not overlap — `docs/research/2026-09-19-background-task-terminal-states.md`), and the whole-transcript alternative is what V1 caught: one stuck id holds the verdict at `waiting` for the rest of the session and the alert never fires again. Rejected by the controller. Run 1 [high]: the prior-report exemption suppresses a stop after any tool-free turn, so a worker told "fix the findings" that answers with text and stops gets no alert — the worker's own round-1 P1 dispute. Codex recommended requiring a fresh `SendMessage` every peer-started turn, which reinstates the measured 6-false / 0-true case; the two transcripts are mechanically identical and nothing separates them without reading intent. Rejected by the controller. No second run: no code changed after the pass, and the head sha was unmoved at `1508412`. **Codex's one real contribution was not a finding.** Its second recommendation named an explicit, machine-identifiable loop-closure signal — a marker a controller emits when its message closes a loop — which separates the two cases mechanically, the thing the worker and controller had both concluded could not be done, and would bound the task set without a status source. Recorded on #900 with Codex credited. The worker's own caveat stands: the marker only works while controllers emit it, so the design needs a stated default for an unmarked message, and that default is today's trade-off again. Merged on Chris's ruling that AC1 (no alert with an outstanding job) is the criterion to fail rather than AC4 (a genuine silent stop still alerts), the two being unsatisfiable together on transcript-only evidence.
| agent-skills#887 | #908 | 1 | 0 | 0 | Run 1 [medium]: the new `Tip` field required a sha and named no source, one bullet below a `CLEAN observed at` bullet that binds its sha to step 5's `headRefOid` and explicitly forbids a local read. So a worker with an unpushed commit, a stale worktree, or a branch moved since its last remote read could report a syntactically perfect tip that is not the PR's head and classify commits against the wrong base, omitting commits genuinely on the PR — leaving the controller to reconstruct remote history, the exact cost the ticket exists to remove. The contract closed the hole for one field and left it open for the next (fixed in `f966d36`: tip is the same `headRefOid` read after the final push, local `git rev-parse HEAD` named and forbidden, and every commit past the reviewed sha carries its own sha beside its diff class so the list is checkable against the PR). Worth noting the worker's own report was correct and the gap would not have caught it — it gave `43e9fc3` for both fields unprompted; the finding is about what the contract permits, not what this worker did. Run 2: approve, no material findings. Controller verified the guard asserts the source rule rather than the field label (`check_in` on "the same \`headRefOid\`", "never your local \`git rev-parse HEAD\`", "its own sha beside its diff class") and ran all six `implement/*.test.sh` green before merging.
| agent-skills#890 | #906 | 2 | 0 | 1 | Run 1 [high]: `blocked_by_section` scanned every line for a heading and every line for an inline form, with no fenced-code awareness and no restriction of the inline form to the body preamble its own grammar defines — so a ticket that merely *quotes* a template (`to-tickets/SKILL.md` ships a fenced `## Blocked by`, and any ticket quoting the new grammar carries one) classified as **unblocked** rather than **unresolved**, the exact false-ready dispatch the ticket exists to prevent (fixed in `34f1ed4`: fenced regions skipped, inline form confined to the preamble — tightening the code to the documented grammar rather than moving the contract). Run 2 [high]: **that fix reintroduced the bug it fixed.** `unfenced` stored only what `_FENCE` captured, three characters, so a ```` opener was recorded as a ``` fence and the first inner triple-backtick line closed it, handing the rest of the quotation back as live document — and a four-backtick fence is specifically what quotes content containing triple backticks. The controller reproduced it against the worker's own head before relaying: a fully-quoted `## Blocked by / None` returned `unblocked` (fixed in `b1e7e9d`: CommonMark closing — same character, run at least as long, no info string on a closer). Note the parity trap the controller flagged and the worker confirmed: with an *even* number of inner fence lines the heading falls back inside the fence and the bug hides, and two of the worker's first fixtures passed against the broken code for that reason. Disputed, premise incorrect: run 1's [medium] claim that `gh api --paginate` without `--slurp` makes a queue past 100 issues unreadable. The worker measured it (`per_page=1` over 13 issues, one merged array, `json.loads` fine on gh 2.95.0) and the controller reproduced it independently (14 issues, one leading `[`, zero `][`) — `--paginate` merges array pages and the reader was never failing. **The controller had "confirmed" this finding from `gh api --help` rather than by running it, which is the `--help`-as-source failure of #875 and story 38 repeated inside the review loop itself.** The `--slurp` hardening shipped anyway as defence, not as a bug fix. Controller re-ran its own repro against the final head (four-backtick, tilde, info-string, live-section and silent-body cases) plus 47 frontier tests before merging.
| agent-skills#884 (clump: #912) | #916 | 0 | 1 | 0 | Run 1 [medium]: `reference_label` reduces both `§ The merge step 6` and `§ The merge step 8` to `The merge`, so a nonexistent step number passes the gate. True — and word-for-word gap 1 of **#913**, which this build filed at its round-1 dispositions from its own C1 and P1, before the pass ran and listed in the PR body at PR-up; hence also-found-by-Claude, and the worker flagged the classification itself. Disposed as **filed** by the controller, over Codex's no-ship: nothing is stale today (all three references were proved to resolve against the real `### The merge` heading, so the gap is future drift), and the catching power Codex said was removed was accidental — before the fix `§ The merge step 8` failed, but so did every other step reference, and a gate that fails all of them distinguishes none. #913 names the cheap recovery (require an `<n>.` list item under the resolved section). No second run: the only change was a PR-body edit and the head sha never moved. **First production use of #889's clump path**: the controller found #912 to be a duplicate of #884 — filed the same morning under Chris's account, same three errors, same file, same two options — had `Closes #912` added to the body, and the single merge closed both while `merge-cleanup` cleared the union, reporting `cleared #884` alone because #912 was never claimed. Ticket outcome worth recording: #884 was filed as three stale `§` references and there were none; all three were symptoms of one grammar bug, and no document was edited.
| agent-skills#883 | #917 | 0 | 0 | 0 | Run 1: approve, no material findings. Round 1 gave 9 findings across the three axes, 5 fixed and 4 disputed, and the verification pass upheld every dispute. The build's own C1 was the one that mattered and Codex had nothing to add to it: the first fix took only the `git` call out from under `set -e`, leaving the record's own `mkdir` able to abort a completed install on an unwritable `~/.local/state`. Ticket outcome worth recording: #883 was filed as two failures and they had one cause — `flow/lane-install.sh` ran `git rev-parse HEAD > "$state_dir/build-sha"`, which under `set -euo pipefail` exited the installer 128 *after* both binaries and every symlink were already in place, while the redirect captured the literal `HEAD` that `rev-parse` echoes on failure and handed `merge-cleanup` a garbage baseline. The empty-`claude/agents` guard was already on `main` and working; that case failed only as a cascade. Recording it because the same cascade produced a second ticket (#912, closed as a duplicate of #884) and a third independent report inside #884's own build. Controller verified `flow/install.test.sh` ALL PASS (15 cases, was 2 FAIL) on the branch, and `bash tests/all.sh` on `main` after the merge: **48 suites passed, exit 0 — the repo's first fully green gate that day.**
| agent-skills#888 | #918 | 1 | 0 | 1 | Run 1 [high], **disputed, premise measured false**: that `<default>` advancing while `headRefOid` holds still changes the review input, so the skip merges on a stale premise. The controller measured it instead of reasoning about it — the branch head against five successive real `main`s (`9a0d117`, `64f01fe`, `33fd10c`, `f08bd9a`, `ae1db1e`) gives the same merge-base `9a0d117` and the same three-dot diff, 2 files / 37 insertions / 3 deletions, every time: a fixed head pins the fork point, so the base gaining commits cannot move `origin/<default>...HEAD`. (A first comparison against `b97e4ed` appeared to confirm the finding and was wrong — that sha is an *ancestor* of the branch, not a plausible old base. The bad comparator produced a confident answer agreeing with Codex.) The measured boundary was carried into the doc anyway: the same comparison taken two-dot reads 18 files and 1080 deletions of other people's merged work. Documentation taken in `c78ab86` — the rule now rests on the merge-base a fixed head pins rather than on the sha incidentally, and names the one shape that would break it. Run 2 [high]: **the skip ignores the other half of its own input.** Step 3's `body_file` is defined at line 369 as the ticket body *and comments*, so a requirement commented onto the ticket between passes changes what a rerun reads while the sha sits still — and #882 landed precisely so a worker treats comments as requirements. Real, in scope, and fixed in `78e5851`: the skip now takes `headRefOid` *and* a `sha256sum` of the rendered body-and-comments, either moving runs the pass, with a guard for the unmoved-head-changed-ticket case. **The finding also caught the controller**: all eleven Codex invocations that day (#886, #889 ×2, #887 ×2, #890 ×2, #884, #883, #888 ×2) built their body file with `gh issue view <n> --json body --jq .body` — body only, against a step naming both — so the lane was skipping on an input it was not feeding. Recorded in `implement/SKILL.md` beside #559 and #877 as the ticket half's own incident, and it is #880's defect surfacing on a second path nobody had filed. No third run: the step's ceiling is two, and the controller declined to read its own run as outside it, reading the delta by hand instead. Worker note carried forward: writing the incident down verbatim tripped #877's own `check_absent` guard, which reads the file rather than the intent and cannot tell a citation from a use.
| agent-skills#891 | #920 | 3 | 0 | 1 | **The trial's strongest run.** Run 1 [high]: `read_text` read only `MAX_BYTES` (1 MB) and returned the partial content as a complete scan, its docstring defending the choice as a case "not worth protecting" — but a directive past the cutoff is a missing edge, and a missing edge is two workers in the same files, the #781 cost this ticket exists to prevent. The controller measured the premise instead of accepting it: `visual-teach/assets/mermaid.min.js` is **3,566,058 bytes**, a tracked file already three times past the cutoff in this very repo, and minified bundles are a single line so the truncation point is arbitrary with respect to content. Run 1 [high]: `read_text` returned `None` both for genuinely non-text content and for any `OSError`, and `include_edges` skipped both identically — so a permission change or a concurrent replacement produced a precise-looking closure with a hole in it. The module was inconsistent with itself, refusing to guess about a directory it could not list while guessing about a file it could not open. Both fixed in `bcc762c`, fail-closed: whole files scanned at any size, a 32 MB memory ceiling that *fails the resolve and names itself* rather than truncating, `None` meaning only "not text", `OSError` raising `ClosureError`. The worker chose scan-whole over the controller's alternative of fall-back-and-announce, reasoning that dropping a repo into conservative clumping over one large tracked file loses precision on every candidate in it — correct. What made these two findings decisive is that the code argued against itself: `scanned_lines`'s docstring three lines above names under-clumping as the direction that must never happen, and both defects under-clump. Run 2 [medium]: `repo()` called `tempfile.mkdtemp` with no teardown — measured by the controller at **1,863 leaked `closure-fixture-*` directories, 84 MB**, growing forty per run on a shared box, against Chris's standing rule that run output never goes in `/tmp`. Fixed in `fd502fd`: teardown in a `finally` so a failing assertion cannot leak, permissions reopened first since two tests remove them, and a surviving fixture reported rather than ignored. Controller verified the close: 42 tests, fixture count `before=1885 after=1885`, delta 0. Two of Codex's claims here were wrong and were corrected rather than relayed — there is no large-file fixture (the over-limit test passes `limit=1000`, so it is a kilobyte; the leak is inodes, not gigabytes), and "presently cannot complete here" was its own sandbox, not this box. Run 2 [high], **disputed**: that the resolver is not wired into a runnable exploration path. Correct on the facts, wrong about whose ticket — `burndown` is parked and says nothing in it is executable policy until #899, the loop that calls `clumps()` is #893, and nothing schedules a worker off declared seams in the meantime because nothing schedules a worker at all. Same disposition as the build's own P1. No third run: ceiling is two, delta read by hand.
| agent-skills#911 | #921 | 2 | 0 | 0 | Run 1 [medium]: `blocked_by_section` returns the **first** visible `## Blocked by`, and this skill appends its declaration at the *end* of the body — so a finding whose evidence quotes another ticket unfenced overrides the declaration just emitted. The controller reproduced it against the reader as merged at `64f01fe` and it is worse than the finding states: the quotation wins **and** the section runs past it to swallow the following prose, because a section ends only at the next heading, so the ticket reads as blocked by `#906` — a number it never claimed — rather than merely failing to classify. Confidently wrong beats unresolved for cost: it parks a ticket behind an unrelated blocker. Fixed in `49ea01a` (pasted evidence must be quoted, `> ` per line or a closed fence), with the reader's half filed as **#922** (two visible declarations are ambiguous, and ambiguous is `unresolved` — the same posture the reader already takes toward silence). **The worker corrected the controller here**: the controller's remedy offered "fence it, indent it, or refuse it", and indenting does nothing — `_HEADING` is `^[ \t]*#{1,6}...`, so a four-space-indented heading still wins. The worker measured the three forms before writing rather than implementing what it was handed, and the controller confirmed. Run 2 [medium]: the skill stated that where `gh` rejects `--blocked-by` the body section still records the relationship, and gave no path that reaches that state — the create carried the flag, and a create that fails files nothing. A stated fallback with no mechanism; the third instance of that defect class in one day (#888's incidentally-justified rule, #877's guard that cannot tell a citation from a use). Fixed in `1e79579`: create carries body and section, the native edge is a second `gh issue edit --add-blocked-by` over the issue just printed, and an edge failure is spoken rather than swallowed. Codex's framing was corrected rather than relayed — the flag was bracketed as optional, and `gh` 2.95.0 on this box has both `--blocked-by` and `--add-blocked-by` (verified), so the real trigger is an older `gh` or a tracker without dependency support. Standing note: **three defects were found in #890's grammar within hours of it merging, all by tickets written around it** — #919 (no cross-repo blocker form), #922, and the indentation gap. The grammar needed its emitter, its reader and its degraded mode specified together rather than a ticket each.
| agent-skills#892 | #924 | 4 | 0 | 2 | Run 1 [high]: a vanished clump counted as a free slot — reproduced by the controller at 3 slots / 1 live / 1 vanished giving `free: 2`, so a resumed controller refills into a slot whose worker may still hold its tickets, contradicting the ticket's own reference. Run 1 [high]: every read-modify-replace went unlocked, so a landing's sha could be overwritten by a stale in-memory `landed: null` and the resumed controller re-dispatches work that already merged. Run 1 [medium]: `load` validated keys but not field types, so `"slots": "2"` passed and `reconcile` then raised an uncaught `TypeError` — reproduced verbatim — defeating the documented clean-refusal guarantee at the one moment recovery needs a diagnostic. All three are one failure wearing three hats, **duplicated work after a restart**, which is the scenario #892 was written from: Codex found three independent routes to it inside the file built to prevent it. Fixed in `4b99388` (`held` = live + vanished; `flock` on `<run-id>.json.lock` beside the file, since `start` takes the lock before the file exists, with a racing-writers witness; full field validation). Run 2 [medium]: **the F3 fix introduced round 1's F4 on a new surface** — `float(os.environ.get(...))` outside the error boundary, so `BURNDOWN_RUNFILE_LOCK_TIMEOUT=30s` crashed recovery with an uncaught `ValueError` (reproduced). Second time in the trial a fix reintroduced its own bug class, after #890's fence fix. The controller ruled sweep-not-patch and named `runfile.py:120`'s identical shape on the test-only delay variable, which Codex had not found; fixed in `75f2413` with one `env_number` door for every environment read — verified by the controller across `30s`, `inf`, `nan`, `-5` and empty, each refused by name except empty, which reads as unset by prior ruling. Disputed twice, same finding: that resume never re-announces. True of `runfile.py` and not its defect — a Python module cannot call `SendMessage`; `reconcile` returning the `announce` bucket is the division of labour, #893 owns the loop that sends and #923 the address resolution, which the controller measured as two hops because an address cannot be stored, only resolved. Rather than dispute it a third time when #893 runs, Codex's own recommendation was written onto **#893 as an acceptance criterion** (one message per live, unlanded worker, against a stub messenger). Worker practice worth recording: its mutation of the lock timeout **hung the suite instead of failing it** — it noticed, bounded the wait, made it fail by name, and re-witnessed, catching a flaw in the guard by mutating the guard.
| agent-skills#893 | #926 | 1 | 0 | 2 | Run 1 [high]: `paths()` called `set()` on any truthy `closure`/`files` value, so a malformed string closure became a set of its characters and intersected no real path set — the clump read as colliding with nobody and the loop dispatched a second worker into a file a live workspace held. The controller reproduced it end to end through the CLI rather than by reading: two clumps both naming `shared.py`, differing only in `"closure": "shared.py"` versus `["shared.py"]`, gave `dispatch  #1  #1` against `held      #1  by #2 in /w/2  over shared.py`. `read_clumps` validated `tickets` hard — list, non-empty, positive non-bool ints — then called `paths(entry)` for its side effect, and `paths` accepted anything truthy; the reader's own docstring names "a hand-built or half-written file" as the normal case, reached "while a controller is recovering", so the input that triggers it is the input the reader was written for. **The same fail-open class #891 closed in `closure.py`, reappearing one layer up**, in the function whose purpose is to prevent two workers in one file — which is why the controller spent the round on it rather than filing it. **The worker improved on the controller's remedy**: told to validate inside `read_clumps`, it put the guard in `paths()` instead, that being the one chokepoint both the CLI and the in-process callers cross, so `refill` and `frontier` called directly cannot slip past it either; and it made `closure: []` refuse rather than fall through to `files`, reading a resolution that produced nothing as an error instead of a silent downgrade. Fixed in `47702c4`, verified by the controller across four forms — string, well-formed list, empty list, list holding an object — each refusing by name at rc=1 except the well-formed list. Run 1 [high], **disputed**: that the written loop orders dispatch before its safety gates, step 4 dispatching while closure freshness is step 5 and the box check step 7. Codex read the step numbering as the execution sequence; step 5 reads "**At each dispatch**, re-resolve the closures" and step 7 "**Box check before every dispatch**", both scoped as prerequisites of every dispatch, step 4's included. Run 1 [medium], **disputed, deferred not dismissed**: that resume announcement is not idempotent if `send` delivers and then raises, so a retry double-announces. Real in principle and out of this ticket's scope — the fix needs a stable message id and an acknowledgement, which is #923's seam; `announce` keeps taking the messenger as an argument. Both disputes were ruled by the controller without the worker and recorded in the PR body verbatim as rulings rather than rebuilt. Controller note on its own conduct: the body file for this pass was built with `--json title,body,comments` and checked non-empty (68 lines) before the run, the practice #888's run 2 forced. Ticket outcome worth recording: #893 is map #776's choke point, and its landing unblocked **four** siblings at once (#894, #895, #896, #897), leaving only #899 behind them.
| agent-skills#894 + #895 | #929 | 2 | 0 | 0 | Run 1 [high]: `--declared` was `default=""`, so `parse_declared("")` gave `{}` and `core_room` charged nothing — a dispatch where the controller did not re-supply every declaration by hand treated every outstanding worker as consuming exactly its own slot. **The file argued against itself twelve lines above the flag**: `--processes` and `--committed-gb` are `required=True` under the comment "an optional flag is the step a controller forgets", and `--declared` was the one core-budget input left optional, for the exact reason given for making the others required. One layer up, § Liveness forbids the same collapse in words — a worker that launched none *says so explicitly*, **silence is not zero**, with a guard needle on the phrase — while the reader's own default read silence as zero. The second half was the expensive one: the declaration lived nowhere but that argv, absent from the run-file schema, so a controller restarting mid-run could not recover an in-flight 8-core job's hold and would dispatch into the contention #351 produced (a run with a free slot dispatching into a box at 25.8 load). Fixed in `56605402` by **deleting the flag entirely** rather than making it required: per-clump job state moved into the run file (`runfile.py job <run-id> --clump <n> --cores <k> | --none | --done`), `dispatch` derives holds from that state, and a live clump with no record is refused **by name** instead of charged zero. Run 1 [high]: `sweep` iterated workers serially with a synchronous `subprocess.run(..., timeout=10.0)` per probe, so N hung panes held the controller inside one tool call for up to 10N seconds — 30 to 50 s at this map's wave size. The docstring named the invariant it broke: the sweep "is never a timer: a controller inside a tool call hears no worker at all (#778)", and #894's own criterion says neither a timer nor a blocking call. Fixed: one deadline for the whole sweep, unreached slots reported as `unswept` and collected on a later wake, so the worst case is a constant rather than a function of live workers. Both findings are the same shape as the ticket's subject — **an absent answer read as a benign one** — which is the class this whole day turned on. |
| agent-skills#896 | #930 | 2 | 0 | 1 | Run 1 [high]: with an outstanding question `landing` printed the full step list — owed answers, then `merge`, then `cleanup` — and exited 1. The exit code refused; stdout did not, handing the `cleanup` token to any wrapper or tail-reader on the one path where cleanup destroys the channel the answer is owed on. That is #454 exactly, the incident the ticket was filed from. The inline comment defended the output ("a controller refused cleanup needs the answers it owes, not only the word 'no'") — right goal, wrong shape. Fixed: owed steps plus an explicit refusal, no bare `merge`/`cleanup` tokens, with a CLI test asserting `cleanup` is absent from stdout whenever questions are outstanding (the old test accepted the unsafe output and had to change with it). Run 1 [medium]: the rebase recipe in `references/merge-tail.md` read `git add -A && git rebase --continue` — staging every tracked modification and every untracked file in the workspace, in a procedure that **ends in a force-push**, so a stray diagnostic script, a local config or a credential is published as part of a collision recovery. Fixed: stage only the named paths, `git diff --cached` before continuing, and the prose "**Never `git add -A` here**" with its reason — a recipe written down is run verbatim under pressure, which is when nobody checks. Run 1 [medium], **disputed**: that the canonical loop does not point to merge-tail. The pointer was in **PR #929**, written there at the controller's direction because `burndown/SKILL.md` was held by the #894 + #895 clump for the whole wave; both PR bodies record the split. First instance of the cross-ticket false positive later filed as #941. **Controller finding, not Codex's**, fixed on this PR rather than filed: this worker's own PR-up said `Parallel jobs: none` because "the three review agents and the verification pass are subagents, not box processes" — the first use of the field this PR introduces excluded the exact fan-out that had been blamed for the day's overrun. § The PR now defines the field as any process the worker causes to exist beyond itself, **every subagent included**, so `none` means none. (The overrun itself was later shown to be a controller measurement error — see the #898 row.) |
| agent-skills#897 | #931 | 3 | 0 | 0 | Run 1 [high]: the generated closing ticket told a worker to run `/multi-axis-code-review` over a list of merge SHAs, but that skill takes **one** fixed point and builds `git diff <fixed-point>...HEAD`; it cannot consume disjoint landed commits. So the ticket instructed a procedure no skill could execute and left the worker to invent a range — the shared-`main` failure the criterion exists to prevent. **Fourth instance of one class in this trial**: a stated fallback with no mechanism behind it (#888's incidentally-justified rule, #877's guard that cannot tell a citation from a use, #911's `--blocked-by` fallback no code path reached). Fixed in `f8872505`: `_review_procedure` emits an executable detached-worktree sha-list recipe instead of naming a skill that cannot take its input; a sha-list input mode for `/multi-axis-code-review` was explicitly **not** built here. Run 1 [high]: `seam_of` read `seam or found.get("seam")`, so the exploration pass's result silently overrode the repo's `AGENTS.md` declaration — while `references/closing-ticket.md` stated the opposite, exploration supplying seam data only when a repo declares nothing. Doc and code disagreed inside one PR. Controller ruling: **the declaration wins** — a declaration an inferred value can silently override is not a declaration — and where both are present and differ, refuse with a diagnostic naming both rather than selecting one. Run 1 [medium]: `check` returned `[_verdict(_validated(d, numbers)) for d in decisions]`, and `_validated` raises on the first bad decision, so one unrelated stale `built_by` naming a ticket outside the spec suppressed **every** real `found: differs` — the one verdict that reaches Chris. Intent fail-closed, effect a blind spot: the check exists to surface contradictions and one malformed neighbour silenced all of them. Fixed with per-decision errors plus a mixed fixture proving a real contradiction survives a malformed sibling. |
| agent-skills#910 | #940 | 0 | 0 | 1 | Run 1 [medium], **disputed**: "Burndown controller instructions omit the new spec bucket." The sentence existed, on `implement-898` at `8be20cc`, verified in that worktree at `d41a5fb` — four-bucket list present, `implement-dispatch --spec` present. `burndown/SKILL.md` was held by another worker for the entire wave, so the wording was drafted here and handed across at the controller's direction, the fourth such cross-ticket line in map #776, recorded in both PR bodies. **Second instance of the same false positive in one map** (after PR #930's merge-tail pointer, which was sitting in PR #929), which is what made it a ticket rather than a dismissal: a review scoped to one branch cannot see a cross-ticket split, so any wave that splits a file across PRs manufactures this finding once per split. Filed as #941. No round 2 — the ceiling is not spent on a false positive. Worth recording from the build rather than the pass: the C1 precedence ruling was implemented one step past its specification, the spec override firing only when `rank` returned `unblocked`, or `unresolved` with nothing stated, so `blocked` outranks `spec` **and** an unreadable blocker declaration stays `unresolved`. Silence was the case ruled into the bucket; a stated prerequisite that could not be resolved is not silence, and an unknown prerequisite is not a met one. |
| agent-skills#937 | #943 | 1 | 0 | 0 | Run 1 [high]: the captured diff was keyed on the issue number alone under a repo-wide shared cache dir, with a truncating write — so a retry, a verification round begun while a round-1 axis was still reading, or another worktree on the same issue replaced the file under the first invocation's readers. The fallback did not catch it: `diff-reviewer.md` re-derives only when the file is **missing or empty**, so an axis handed a replaced-but-non-empty patch reads it as valid while still holding round 1's command, commit list and spec context — a review that looks complete, against the wrong tree, with no error anywhere. **This ticket's own defect class, one layer out**: its round-1 C1/S1 was an unexpanded `<n>` placeholder that the write and the `[ -s ]` guard agreed on, so the guard passed and every axis silently fell back. Same shape — a guard that tests for absence when the hazard is wrongness — and the worker said so itself: "it is the second time this shape got through me on this ticket." Fixed in `f2893dc`: the name carries the short revision plus a per-invocation nonce, the diff is written to `mktemp` and published with `mv`, and every axis of one invocation is handed the exact path the block printed rather than a pattern. **The regression test is the part worth copying** — it extracts the fenced block out of `SKILL.md` and executes it rather than retyping the shell, because a copy in the test file would pass forever while the doc drifted; against the pre-fix doc it reproduces the finding exactly. The worker also asserted rather than assumed that the 14-day `find -mtime` sweep in the same block still collects captures under the new key. Controller note: this was the **first pass to carry #941's sibling-branch appendix**, and no cross-ticket false positive followed. |
| agent-skills#898 | #945 | 0 | 0 | 1 | Run 1 [high], **disputed**: "Tier tagger is unreachable from the active lane." Codex observed `disable-model-invocation: true` and the parked banner in `burndown/SKILL.md`, found no executable caller of `tier.py` outside its own tests, and called it a no-ship. Correct about the tree and wrong about the ask: every ticket in map #776 lands into the parked skill by construction — `frontier.py` and `loop.py` are unreachable by the same route — and #899, the map's closing ticket, is the unparking plus the supervised end-to-end run, with #898 in its `Blocked by` and a one-command reachability check in its acceptance criteria. Codex's own recommendation offered the alternative ("if the lane is intentionally parked, defer this change rather than claiming the behavior is live"), and nothing in the PR body claimed the behaviour was live. Building the wiring here would have duplicated #899 and unparked the skill ten tickets early. **Same class as the split false positive, second line of the same gap**: #941's appendix names open sibling branches, which suppressed the cross-ticket shape on this pass (the frontier paragraph, the exact shape that fired on PR #940, drew nothing), but says nothing about the *posture* the code is deliberately in. Recorded on #941 as a second required line: when a module lands ahead of its own activation, the focus text says so and names the ticket that activates it. Without it, every PR in a staged rebuild pays one `[high]` whose remedy is "do the closing ticket early" — this trial has now paid it **twice**, here and on #891. Worth recording from the build: the worker's verification pass **corrected one of its own dispute reasons**, catching that it had cited CLAUDE.md's "`--dry-run` when one exists" as a mandate to build a preview when the line is an obligation on whoever hands a command over; the dispute was re-grounded on cost and the bad citation kept out of the controller's hands. The unreviewed tip `45b04a2` was a carry-over the verification pass raised on its own diff: a dry run printed `labels written:`, the same heading as a real run, so a controller reading a preview would take the tier as already fixed on the tracker. |

## Keep/drop recommendation (2026-09-20, controller of map #776)

The five-row trigger fired and overshot: **nine rows** landed after the
2026-09-19 line — `#891`, `#892`, `#893`, `#894 + #895`, `#896`, `#897`,
`#910`, `#937`, `#898` — because map #776's tail ran six merges in one day.

| | rows since 2026-09-19 |
|---|---|
| codex-only, confirmed | **16** |
| also found by Claude | **0** |
| disputed | **8** |

### Recommendation: keep.

The trial's own keep condition is "at least one `codex-only, confirmed`
finding would have shipped a real bug." This window cleared it many times
over; three are worth naming because each was a **fail-open in the exact
code written to fail closed**:

- `#891` — `read_text` truncated at 1 MB and returned the partial content as
  a complete scan, in the closure resolver whose purpose is to stop two
  workers landing in one file. The repo already tracks a 3.5 MB file.
- `#892` — a vanished clump counted as a free slot, so a resumed controller
  refills into a slot whose worker may still hold its tickets. Reproduced at
  3 slots / 1 live / 1 vanished giving `free: 2`.
- `#893` — `paths()` called `set()` on any truthy closure, so a malformed
  string closure became a set of its characters and intersected no real path
  set: the clump read as colliding with nobody. Reproduced end to end through
  the CLI.

**Zero findings in this window were also found by the Claude axes.** That
column has been 0 for nine consecutive rows. The pass is not repeating the
three axes; it is reading the code for a different failure mode, and on this
map it read the one the axes are weakest on — the benign default.

### The cost, stated plainly

Eight disputes in nine rows is real overhead, roughly a controller round each.
But **half of them are one bug in the pass's inputs, not eight independent
misreads**: `#896`, `#910`, `#898` and one of `#891`'s are all the pass being
unable to see context the controller had — a line split onto a sibling branch,
or a module deliberately landing ahead of its own activation. That is #941,
whose remedy is a paragraph of focus text the controller already knows at
dispatch time. It was trialled twice on 2026-09-20 (`#937`, `#898`), and both
times the shape it covers produced nothing.

So the honest read is: keep the pass, and land #941 before judging the dispute
rate again. If #941 works, this window's eight disputes would have been four.

### One thing the trial should record about itself

Twice now — `#890`'s fence fix and `#892`'s F3 fix — **a fix reintroduced its
own bug class on a new surface**, and Codex's round 2 caught it both times.
`#937` is the third instance inside a single ticket (the placeholder guard and
the cache key were the same shape, and the worker said so). The two-round
ceiling is earning its keep specifically on re-introduction, which is not what
it was put there for.

Filed from conversation — controller, map #776 merge tail.
| agent-skills#899 | #947 | 0 | 0 | 0 | Run 1: **approve, no material findings** — the map's closing ticket, unparking `burndown` and `implement-spec` and adding the finish-line check. Worth recording what the pass did rather than only its verdict: it ran `tests/lane-unparked.test.sh` itself, grepped the tree for both parked marks with the fixture excluded, read both skills end to end, and concluded independently that the check's reachable-doc and module assertions are **fail-closed for missing modules and dangling links**. That is the exact property the correctness axis had forced into the diff, and an independent reader reaching it by execution is stronger evidence than the assertion. It could not run the full gate — read-only sandbox, a harness needing `/tmp`. **First trial of #941's second context line (posture)**, and the PR most exposed to the false positive it exists to stop: the parked state is literally what the diff removes, and the same shape had produced a `[high]` no-ship on PR 920 (#891) and PR 945 (#898). It did not fire. Two findings from the build, both the day's defect class landing inside the check built to prevent it: the first draft's reachability assertion was a substring search over concatenated prose and `burndown/SKILL.md` names all three module paths itself, so **it passed with all three modules deleted**; and the sweep discarded `grep`'s exit status, so a tree with 380 unreadable files printed "409 files swept" and a clean lane. The worker also checked one of its own dispute reasons, found it false, and said so. Incidental find only this check could produce: `burndown/references/spec-handoff.md` had been unreachable from any skill since the parking commit, unnoticed for weeks, because nothing could answer "is this document reachable". |
| agent-skills#941 + #942 | #950 | 1 | 0 | 0 | Run 1 [high]: the retry that runs **because** the early pass degraded was the one path with no exit-status capture, no record, no validation and no duration row. The PR's new early-launch block captures `status=$?` and writes ticket, status, launch and completion shas, body hash and timestamps; the gate's foreground block — reached when an early record is absent, unreadable, errored, raced or stale — was the legacy one, writing `out_file` and nothing else. So a retry that errored would be posted and read under "No material findings → go to step 4" because nothing looked. The file argued against itself: "**Every run records its duration**, collected or discarded" sat three lines above a block that could not. **Fifth instance in this trial of a stated fallback with no mechanism behind it** (#888, #877, #911, #897, this). Fixed in one commit, not disputed: a single execution block with `phase=early\|gate-retry\|second`, the two-block structure gone, the retry answering to the same five refusals, and a `gate-retry` record that is itself refused ending step 3 as a visible `Codex pass skipped: <why>` on the PR — one escape, stated rather than inferred from silence. The phase is in each filename so a retry cannot overwrite the record that justified it. The worker went further than asked and moved the files out of `.scratch/` to `~/.cache/agent-reviews/<repo>/`, removing the hazard the `rm`/`rmdir` apparatus guarded (#835) rather than guarding it again. Separately, and the reason #951 exists: the worker disputed #942's `--background` benefit after finding `handleReviewCommand` parses `background` and never reads it while the usage text advertises it — **a documented flag accepted and ignored**, verified from source by two review axes, the worker, and the controller. |
| agent-skills#932 + #938 + #939 | #953 | 1 | 0 | 0 | Run 1 [medium]: the new isolation recipe had no EXIT trap, so cleanup ran only after the covering suite passed — and **a witness check that works makes that suite fail**, so the ordinary outcome leaked a registered worktree every time, against the recipe's own promise that the checkout is left exactly as found. The new witness test exercised only the success path and could not see it. Fixed: trap armed immediately after `worktree add`, cleared after normal cleanup, and the fixture now runs the recipe a second time with a deliberately failing covering suite. **The clump's real story is four hollow witnesses the worker caught in its own work, in a diff whose subject is hollow witnesses**, every one found by building the test rather than reading the code: (1) the first `cp -a` mutation turned the suite red because the extraction keyed on the line the mutation deleted, so the block was never found — true status, false reason, one step from being reported as proof; (2) the ordering fixture named its shas in the answer's own order, so replacing the sort with `cat` stayed green; (3) a refusal guard whose only witness requires root, so the case it guards cannot be reached in the test environment; (4) writing the round-2 test, the deliberately-failing run killed the suite under `set -e` with no message, because a run that is *supposed* to exit non-zero still trips the shell. Measured rather than argued, and the strongest single fact in this trial for what the witness check is worth: the verification pass re-ran the same `cp -a` mutation against the **pre-change** fixture and it came back **green** — the old fixture could not see the index leak at all. The field evidence for #939 came from `skills-899`, which followed the brief's "scratch copy of the tree outside the checkout", used `cp -a` on a linked worktree, and had two `git rm --cached` mutations stage deletions in the **real** index, because a linked worktree's `.git` is a gitdir pointer file. That turned #939 from a cost ticket (419 MB, 2m51s) into a correctness one mid-build. |
| agent-skills#869 + #946 | #952 | 0 | 0 | 1 | Run 1 [high], **disputed**: that the empty-directory approval is stale by the time `git worktree remove --force` runs, so a file created in the window is destroyed without `--discard`. The race is real and is **#879**, open since PR #878 — where **the same reviewer raised it, it was disputed, and it was filed rather than built**. Second sighting of an already-filed, already-disputed defect: the pass cannot see the tracker, so it re-raises on the next diff touching the file. **Third false-positive class**, alongside the two in #941, noted on both tickets. The dispute's substance, which neither ticket had stated: the old refusal was never the safer behaviour, because before #946 an empty ignored directory refused, required `--discard` from Chris, and the forced removal still followed — the same check-then-act window with a **human approval inserted into the middle of it**, making it minutes rather than seconds. #869 records six such refusals in a row on 2026-09-16. Codex's recommendation was to reinstate exactly that, which is what #946 was filed to remove on Chris's explicit ruling. What the diff does close is the other half, fail-closed by construction: `files_under` returns `None` when any part of the walk cannot be read, so "observed empty" and "could not observe" are different answers and an unreadable directory still refuses — witnessed by the `None => self.empty_dirs.push(entry)` mutation turning `an_unreadable_ignored_directory_still_refuses` red. The round's own find was better than the pass's: **git quotes any porcelain path holding a non-ASCII byte, a quote, a backslash or a control character, and the quoted form ends in `"`, not `/`** — so matching on the slash filed `café/` as a plain file and never walked it, and **both** bugs survived the fix intact for every name git quotes, including the direction that matters, "1 ignored file(s)" reported against hundreds. A fix landing with that hole is worse than no fix, because the count then looks authoritative. |
| agent-skills#954 | #956 | 1 | 0 | 0 | Run 1: the controller had ruled *against* a per-suite opt-out on the grounds that no legitimate need existed, and the pass found one — Python's `logging.error()` writes `ERROR:root:<msg>` at column 0, and since the gate folds stderr into what it scans, a **passing** `*_test.py` that exercises a logging path would be failed by the new scan. The worker's own code comment already said so; the controller's ruling and the worker's comment contradicted each other and neither noticed. Partially accepted: there are two recovery paths (set `basicConfig(format=...)`, or capture the logger), so the remedy shipped as a named `logging_remedy` in the failure message conditioned on `^[A-Z]+:[A-Za-z0-9_.]*:` rather than as a mechanism. Recorded because the finding overturned a controller ruling, which is the case the pass is least likely to be asked about and most useful for. |
| agent-skills#958 | #959 | 3 | 0 | 3 | Two runs, three findings, **all three the same defect on three different call sites of one guard**, and the strongest evidence in this trial for what the axes systematically miss. The diff makes `loop.py` measure agent processes itself so that an unmeasurable box refuses instead of reading as empty. Run 1 [medium] ×2: `--processes` accepts a negative count, so `box --processes -1` printed `box ok` — and `--processes` is the *documented escape hatch for an unmeasurable box*, so the one path a controller reaches for when measurement fails is the one that silently disarms the cap on a typo; and the failed-`ps` CLI test drove `box` while every `dispatch` test supplied `--processes`, so `dispatch` could stop measuring entirely and stay green. Run 2 [medium]: the round-1 fix's new `dispatch` test covered only the path with room, and `dispatch`'s declared-job early return fires **before** `agent_count()`, so a broken `ps` exits 0 behind "nothing to dispatch". Three disputes, all on the standards axis, all accepted by the controller (`uptime`/`free -g` do have consumers; the five restatements of the agent-vs-OS distinction were each separately requested; the `counter` label has two real values). The worker flagged the run-1 coverage gap itself, unprompted, in its own PR-up — and it turned out to be the load-bearing one. **This is #961**: the axes ask for a mutation of the constraint under test and never ask whether every entry point a guard protects would notice the call being removed. Mutating the guard's body reddens the unit test, the witness passes, and the guard stays bypassable at the only place it matters. |
| agent-skills#957 | #960 | 8 | 0 | 1 | **Four runs, eight findings, every one accepted** — the trial's worst overrun and the clearest case that the two-round ceiling is a cost control and not a correctness gate. Subject: making the witness check's mutations concurrent. Run 1 [high] ×3: an empty output with a nonzero status reported as `red`; two legal ids (`x`, `x.out`) colliding in one namespace so a worktree could occupy a sibling's output path and the suite never start; `cleanup` suppressing every `git worktree remove` failure and then `rm -rf`ing the root, leaving exactly the stale registration its own prose forbids. Run 2 [high] ×2 + [medium]: a pre-suite failure that writes to stderr still read as `red`, because non-empty output is a proxy for "the suite ran" and the wrong one; INT/TERM removing worktrees without signalling or reaping the children still writing into them; **an empty `ids` list exiting 0 as a clean witness check** — a failed enumeration reading as a passing check, inside the tool built to catch exactly that. Run 3 [high] + [medium]: the new marker can certify a suite that never started (a command that does not exist writes the marker, exits 127, and is reported red with "command not found" as the failure message); and `$ids` expanding unquoted so `ids='*'` globs to the checkout's filenames, every one passing the character check, defeating the pre-creation guard that had just been built. One dispute, accepted: removing the INT/TERM trap alone leaves the suite green because this bash runs the EXIT trap on SIGTERM, so the interrupted-run behaviour is witnessed by deleting both traps. **The shape across all four runs is one thing said four ways.** Each round replaced one proxy for "the suite actually ran" with a closer proxy — status, then status plus non-empty output, then a marker written before the command — and each closer proxy had the same hole one step further out. It ended only when the answer stopped being inferred: 126 and 127 are the kernel stating directly that the command never executed. The worker's own contribution was the sharpest instance in this trial: its concurrency fixture's probe ("did the sibling start?") was satisfied by a **serial** run, because the sibling's marker and worktree are still on disk when the second mutation runs — it asserted the artifact of concurrency, and the artifact outlives the thing. A two-phase barrier was needed to make the probe fail on a serial launch. **Ceiling rule changed here**: the controller overrode the two-round ceiling explicitly at round 3, on the grounds that the finding was the ticket's own central property, then capped it at round 4 with a standing rule — anything Codex raises after the final round is a follow-up ticket, not another round. An unbounded review is its own defect. |
| agent-skills#901 | #965 | 0 | 0 | 0 | One run, no findings: approve on the clump-brief additions (`implement_dispatch.rs`), with the guard tests for both the clump and the single-ticket brief noted as exercising both paths. The controller missed the round-1 launch, so this pass ran at the gate as `gate-retry`; the early phase never ran. |
| agent-skills#966 | #967 | 0 | 0 | 0 | One run, no findings: approve on the slot default of 5 across `runfile.py start` and `implement-dispatch --spec`, noting the positive-value guard and the explicit override both survive. The controller held the early launch because the worker announced fix commits at round 1; the pass ran once at the gate as `gate-retry`. |
| agent-skills#948 + #961 | #968 | 1 | 0 | 2 | Three runs. Early pass raced (the worker pushed round-1 fixes while Codex read) and was discarded. Gate-retry [high], **codex-only, confirmed and fixed**: the final capture path was keyed on `$$`, per shell not per invocation, so two same-input captures in one shell shared a path and `mv` replaced a patch a lagging axis could still hold — pre-existing on main, but the PR restated the "per-invocation nonce" guarantee, so the claim would have landed with the hole. No Claude axis raised it; the worker fixed it with the `mktemp` suffix and a same-shell-twice test in both suites. Second pass, two disputes: mktemp-suffix reuse after the move (a random 62^6 name eligible again is the ordinary nonce bound, not a defect), and "the same id as any constraint mutation" read as sharing an id (the witness check refuses duplicate ids; the ambiguous sentence is #969). |
| agent-skills#970 | #972 | 0 | 0 | 0 | Two runs, both approve, no findings: the family/clump split in closure.py and its JSON hand-off to loop.py. The early pass raced on round-1 fixes and was discarded; the gate-retry approved. |
| agent-skills#871 | #974 | 1 | 1 | 1 | Three runs. Early pass raced on round-1 fixes and was discarded. Gate-retry [high], **codex-only, confirmed and fixed**: the new `handed-back` disposition line carried the /file-ticket command as `"command": "<the command>"` with no stated encoding, and that command is a multi-line heredoc with quotes, so a verifier writing the JSONL line by hand could split it and break the id join — no Claude axis raised it; the worker fixed it by stating the field is the command JSON-encoded as one string, with a heredoc round-trip test. Gate-retry [medium], disputed: "origin owner is not Chris" versus "not the caller's gh login" are one predicate under Gate 1. Second pass [medium], also found by Claude: the tally parser drops `handed-back` lines as undisposed — the Claude axes' P2/C1, filed as #973 before Codex raised it. |
| agent-skills#875 + #881 + #949 | #977 | 1 | 0 | 0 | Three runs. Early pass raced on round-1 fixes and was discarded. Gate-retry [medium], **codex-only, confirmed and fixed**: the help sentence hardcoded the seven cache names while CACHE_DIRS was a separate const and the new test pinned two phrases, so a name added to or dropped from the const would leave the test green and the help wrong again — the drift #875 exists to stop; no Claude axis raised it. The worker built the help list from CACHE_DIRS and pinned the sentence; dropping `.mypy_cache` from the const turned the test red. Second pass approved. |
| agent-skills#900 | #979 | 2 | 0 | 0 | Two runs, both codex-only and confirmed. Gate-retry [high], fixed: a poll that was denied or errored still counted as liveness because `$touched` read tool_use inputs alone, so a worker denied a poll and stopping to ask in its pane (the #905 shape from an hour earlier) would have had its stop alert suppressed; fixed by counting a probe only when its tool result exists and is not an error, with denied and errored fixtures. Second pass [high], filed as #981: a timed-out Monitor notification carries no `<status>` and so reads as liveness too — confirmed in real transcripts. No Claude axis raised either. Early launch held by controller ruling. |
| agent-skills#905 | #978 | 1 | 0 | 3 | Three runs (early held by controller ruling). Gate-retry: [high] lock keyed under $HOME, disputed — one user, one HOME on this box, and keying by repository is what lets two checkouts of one GitHub repo share it; [medium] **codex-only, confirmed and fixed**: the overlap test started B only after seeing the lock note, so with the acquisition and its note removed B waited out its ten seconds, A claimed, B refused on the visible label, and the test passed without the lock — the worker moved the barrier to the fake gh read seam and saw the no-lock mutation red 3 of 3. Second pass: HOME scope again, disputed; and the barrier sits at the first read rather than the reread before the edit, so the no-lock witness is probabilistic — filed as #982. |
| agent-skills#880 | #980 | 0 | 0 | 0 | One run at the gate (early held by controller ruling): approve, no findings, on the Codex lane reusing the tested body-and-comment renderer. |
| agent-skills#903 | #984 | 0 | 0 | 0 | One run at the gate (early held by controller ruling): approve, no findings, on the oversized-ticket-number tolerance test in merge-cleanup. |
| agent-skills#915 | #985 | 1 | 0 | 1 | Two runs (early held by controller ruling). Gate-retry [medium], **codex-only, confirmed and fixed**: the new grammar doc said prose after "§ Step 3" makes the reference fail, and on main it joins the label and resolves by prefix — the paragraph was removed, since #913 was rewriting that very locator in a sibling branch. Second pass [high], disputed with a plan: the doc now defers the rule to the checker; the controller adds the paragraph on main after #913 lands, stating the rule the code then has. |
| agent-skills#909 + #951 + #963 | #987 | 1 | 0 | 1 | Two runs (early held by controller ruling). Gate-retry [medium], **codex-only, confirmed and fixed**: the new public-file rule ("any file whose contents become public lives in the worker's own .scratch/") contradicted § The merge, which deliberately keeps the controller's Codex out_file in the review cache and publishes it from there; the worker scoped the rule to worker-authored files and named the exception. Second pass [medium], disputed: "authors" read as excluding tool-generated files — a reading, not a defect. |
| agent-skills#913 | #986 | 2 | 0 | 0 | Two runs (early held by controller ruling), three codex-only findings, two fixed. Gate-retry [medium] ×2, **confirmed and fixed**: fence tracking toggled on any fence marker and forgot the opener's character and length, so a `~~~` inside a four-backtick fence closed it early and a code-sample list item read as a real step; and a colon in a referenced heading name made the locator fail to match, the fallback truncated at the colon, and the step requirement vanished. Second pass [high], filed as #990: prefix matching lets a step resolve in a sibling heading (`# Merge` versus `# Merge notes`). Second pass [medium]: the grammar doc lagged the checker — the controller's own edit on main. No Claude axis raised any of the three. |
| agent-skills#919 | #989 | 0 | 0 | 1 | One run at the gate (early held by controller ruling): [medium] the cross-repo blocked-by edge was only simulated — **disputed by a live probe**: the controller ran the documented `gh issue edit --add-blocked-by <URL>` against a real cross-repo pair, read the edge back (totalCount 1, the other repo's issue as the node), and removed it. The finding was right that nobody had checked; the probe closed it in under a minute. |
| agent-skills#914 | #995 | 0 | 0 | 0 | One run at the gate (early held by controller ruling): approve, no findings, on the ordered prose trimmers in check-section-references. |
| agent-skills#925 | #992 | 1 | 0 | 2 | Two runs (early held by controller ruling). Gate-retry: [high] structural jq equality versus the ticket's "byte-identical", disputed — the transcript stores the harness's serialized object, so structural equality is the implementable contract, now stated in the hook's header; [medium] **codex-only, confirmed and fixed**: the dedupe key truncated the input at 120 characters, so two long same-prefix spins shared one alert record and the second was suppressed — keyed on a digest of the full input now, with a fixture. Second pass: the dedupe identity has no run boundary, so a later streak of the same call after an interrupting call never alerts — filed as #998; and byte-identical again, disputed again. |
| agent-skills#927 | #997 | 0 | 0 | 0 | One run at the gate (early held by controller ruling): approve, no findings, on the end-to-end seam declaration in AGENTS.md and its reader. |
| agent-skills#922 | #996 | 1 | 0 | 0 | Two runs (early held by controller ruling). Gate-retry [high], **codex-only, confirmed and fixed**: the indent bound stopped an indented `## Blocked by` from counting as a second declaration, but section extraction still appended its lines and the blocker scan read `#NNN` out of them, so a quoted blocker after a real `None` still held the ticket — the ticket's own subject, one level down; Codex reproduced it on the branch, and the worker built one shared visibility test feeding both readers. Second pass [medium], filed as #999: an indented fence marker inside a quotation opens a fence and, unclosed, swallows the real declaration (fail-closed). |
| agent-skills#928 | #1001 | 1 | 0 | 0 | Two runs (early held by controller ruling). Gate-retry [medium], **codex-only, confirmed and fixed**: the new closing-emphasis clause consumed `**` only before whitespace, so `- **Directive:**` followed directly by a code span kept the `**` in the directive and resolution ran against a malformed template, under-clumping instead of refusing — the ticket's own colon-inside form, one space away; fixed as a balanced alternative with a no-space regression test. The worker noticed closing_ticket.py keeps the old regex and has the same gap; noted on #1000. Second pass approved. |
| agent-skills#933 | #1002 | 2 | 0 | 0 | Two runs (early held by controller ruling), both codex-only. Gate-retry [high], **confirmed and fixed**: `--in-flight` was optional, an omitted snapshot read as zero live workers, and the new peak reserve admitted a worker the cap would refuse once the live one fanned out — defect class 1 in the reader built to enforce the cap; now required, with an explicit empty list for "none live". Second pass [medium], filed as #1003: a landed clump with a null job record is charged before landed clumps are filtered, so a run-file-style snapshot refuses the tick after every landing. |
| agent-skills#955 | #1004 | 1 | 0 | 0 | Two runs (early held by controller ruling), both codex-only. Gate-retry [high], **confirmed and fixed**: the new backtick stop cut a heading name at its first backtick, so a name holding inline code read as its prefix and stayed green after the inline-code part was renamed — the silent downgrade the ticket exists to stop; no current reference did it (90 swept), which is why the axes missed it; fixed fail-closed with a refusal and a fixture. Second pass [high], filed as #1005: the guard counts single backticks rather than delimiter runs and runs after the prose trimmers. |
| agent-skills#936 | #1008 | 0 | 0 | 0 | One run at the gate (early held by controller ruling): approve, no findings, on merge-tail.md's staging rule reaching the hand-merge case. |
| agent-skills#934 | #1007 | 3 | 0 | 0 | Two runs (early held by controller ruling), three codex-only findings, two fixed. Gate-retry [high] ×2, **confirmed and fixed**, both from the worker's own flagged choice to warn and proceed on a foreign pre-commit hook: dispatch started a worker whose commits the guard never saw, and the warning told the user to merge the guard into their hook, whose marker then made the next dispatch overwrite the whole file — destructive. Controller reversed the choice: fail closed, and lane-owned means byte-identical. Second pass [high], filed as #1009: a foreign hook is accepted on a text match for the guard's name, spoofable, and the executable bit is unchecked. |
| agent-skills#962 | #1010 | 1 | 0 | 1 | Two runs (early held by controller ruling). Gate-retry [high], **codex-only, confirmed and fixed**: a mutmut results file holding only timeout, suspicious or skipped mutants passed the has-a-mutant-line check, produced no rows and exited 0 like an all-killed run — defect class 1 inside the tool built to catch it; any status other than killed, survived or no-tests now raises Inconclusive naming the status and mutant. Second pass [medium], disputed: a malformed timeout env raises a traceback with exit 1 rather than INCONCLUSIVE with exit 3 — loud, not silent. |
| agent-skills#923 | #1012 | 3 | 0 | 0 | Two runs (early held by controller ruling), three codex-only findings, one fixed. Gate-retry [high], **confirmed and fixed**: a controller record with an empty sessionId was read as "no herdr agent" and the brief fell back to the restart-volatile session name — the ticket's own incident, one field away; now a refusal. Second pass [high] ×2, filed as #1013 and #1014: the resume re-announce and the stop-alert hook both still take the herdr name as a literal address. |
| agent-skills#874 | #1018 | 4 | 0 | 0 | Two runs (early held by controller ruling), four codex-only findings, two fixed. Gate-retry [high], **confirmed and fixed**: the installer replaced the five `~/.local/bin` shims before the registry write, so a `reg add` failure left the live handler pointing at a symlink Windows cannot follow — registry now precedes the links, with a failing-registry test. Gate-retry [medium], **confirmed and fixed**: unquoted `.vbs` path in the registered command; quoted, exact-argument assertion, fixture with a space. Gate-retry [medium], disputed: `.pre-flow` overwritten on retry — a retry skips linked names. Second pass [medium], disputed: an `ln` failure after `mv` in the same directory; re-runnable, rollback out of scope. |
| agent-skills#964 | #1043 | 4 | 0 | 0 | Two runs (early held by controller ruling), four codex-only findings, three fixed. Gate [high] ×2 and [medium], **all confirmed and fixed**: cleanup's unlocked read/filter/rewrite of the worker sidecar lost a racing dispatch append (now one exclusive `File::lock()` across the read-modify-write); records keyed by pid alone would restore a dead controller's workers into a reused pid after a restart (now bound to the session's `proc_start`); the restore hook's 15 s timeout was under its own 10 s + 10 s-per-worker query budget (now one 12 s deadline, `unchecked` for a query that never ran). Second pass [medium], filed as #1044: `read` still takes no lock, so a `/clear` inside cleanup's truncate-rewrite window restores nothing. |
| agent-skills#973 | #1046 | 2 | 0 | 0 | Two runs, two codex-only findings, one fixed. Gate [medium], **confirmed and fixed**: the disposition parser coerced any non-null detail with `str()`, so a list-valued `command` tallied as handed-back rather than undisposed; detail is now typed per outcome (controller caveat: `ticket` stays int, since the sidecar writes it unquoted). Second pass [medium], disputed: `ticket: true` passes the int guard because bool subclasses int — no sidecar writer emits a boolean there. |
| agent-skills#969 | #1048 | 0 | 0 | 0 | One run at the gate: approve, no findings, on the call-site-mutation id wording and its witness test. (The same wording had landed at light tier as 66c0a8e and was reverted; this is the code-lane redo.) |
| agent-skills#975+#983 | #1051 | 0 | 0 | 0 | One run at the gate: approve, no findings, on merge-cleanup's refusal when the worktree listing fails and the length-then-text ticket ordering fixture. |
| agent-skills#971+#988+#1003 | #1050 | 1 | 0 | 0 | Two runs, one codex-only finding, fixed. Gate [high], **confirmed and fixed**: #1003's landed-clump filter reached `core_room` and the peak line but not the two `frontier` calls beneath them, so a landed clump's dead workspace still printed `held` and blocked the dispatch; both calls now take `unlanded`. Second pass: approve. |
| agent-skills#976+#1009 | #1053 | 2 | 0 | 0 | Two runs, two codex-only findings, one fixed. Gate [high], **confirmed and fixed**: the round-1 fix had moved pre-commit ownership from byte-identity to a marker-string match, which a foreign hook could carry to skip the takeover — the exact trust boundary #1009 removes, and a reversal of the #934 ruling; byte-identity restored with marker-spoof end-to-end tests. Second pass [high], filed as #1054: an owned wrapper that lost its exec bit is not repaired on a later dispatch. |
| agent-skills#990+#1005 | #1055 | 1 | 0 | 0 | One run at the gate, one codex-only finding, filed as #1056: a code span crossing a line boundary is invisible to the per-line scan, so a `§` inside it is rejected — pre-existing line scoping that #1005's guard made visible; the controller did not spend the fix round on it. |
| agent-skills#993 | #1058 | 0 | 0 | 0 | One run at the gate: approve, no findings, on the end-to-end test of this repo's own AGENTS.md seam declaration. |
| agent-skills#981+#1014 | #1057 | 2 | 0 | 0 | Two runs, two codex-only findings, one fixed. Gate [high], **confirmed and fixed**: the hook dropped a PID-live controller record with no session name, so a report delivered to its socket read as silent and a false stop alert fired — the worker's own comment had argued a nameless session was unaddressable, which every cross-session reply address disproves. Second pass [high], filed as #1059: with two live records sharing a sessionId the first nameless one now wins over the later named one. |
| agent-skills#982 | #1060 | 2 | 0 | 0 | Two runs, two codex-only findings, both fixed. Gate [high], **confirmed and fixed**: the overlap test's fake released its two-party barrier before reading the claim state, so a scheduling gap could still let one run see the other's claim and the witness passed without the lock — the worker's 20/20 measured a tight window, not determinism; the state is now captured before the barrier. Gate [medium], **confirmed and fixed**: a failed read_dir in the marker count read as zero and timed the barrier out silently. Second pass: approve. |
| agent-skills#998 | #1061 | 2 | 0 | 0 | Two runs, two codex-only findings, one fixed. Gate [high], **confirmed and fixed**: a spin that filled the 500-call window or the byte cap flipped its dedupe key to a null run_id and woke the controller a second time; a null run_id now dedupes against any prior sent alert for the same session, tool and input. Second pass [high], filed as #1064: 500+ sidechain tool_use lines can push an alerted spin's first id out of the raw window without nulling run_id. |
| agent-skills#999 | #1063 | 2 | 0 | 0 | Two runs, two codex-only findings, one fixed. Gate [medium], **confirmed and fixed**: `closure.scanned_lines` read a four-space-indented `#include` example in a `.md` doc as a live include edge, so a docs example over-clumped two candidates; the Markdown branch now goes through `frontier.visible()`. Second pass [medium], filed as #1065: `_QUOTED` misses a space-then-tab indent, so a quoted ` \tNone` under `## Blocked by` reads as unblocked — pre-existing on main from #922. |
| agent-skills#991 | #1066 | 2 | 0 | 0 | Two runs, two codex-only findings, one fixed. Gate [medium], **confirmed and fixed**: the new fail-loud guard trusted `source`'s exit status, so a lib that parsed but lacked some `worker_alert_*` functions let both hooks exit 0 with no `not-sent` line; both hooks now `declare -F` every function they call. Second pass [medium], disputed: `lib_missing()` ignoring a failed `mkdir`/append under an unwritable `~/.claude` — the harness's own transcript fails first, and a Stop hook's stderr is not a durable channel. |
| agent-skills#1013 | #1067 | 1 | 0 | 0 | One run, approve, no material findings: `loop.announce` resolves the herdr name at the send boundary and refuses by worker name; the round-1 axes had already driven four fixes (a shared PATH-stub helper, a default-resolver witness, and isolating the two refusal guards under mutation). No second pass. |
| agent-skills#1006 | #1068 | 2 | 0 | 0 | Two runs, three codex-only findings, one fixed. Gate [high], **confirmed and fixed**: the buffered pre-push wrapper ignored `cat`'s exit status, so a truncated ref list could pass the guard with refs unchecked; the copy now fails closed before either hook runs. Second pass [high]+[medium], filed as #1069: the 40-char zero sentinel refuses new-branch and deletion pushes in a SHA-256 repository, and `saw_a_ref` is set before the record is validated so a malformed line exits 0 unchecked. |
| agent-skills#1015 | #1070 | 1 | 0 | 0 | One run, approve, no material findings: the early Codex launch is retired, the pass runs once at PR-up as `gate`, the five refusals and the conditional second pass stand. No second pass. |
| agent-skills#1016 | #1071 | 2 | 0 | 0 | Two runs, two codex-only findings, both fixed. Gate [high], **confirmed and fixed**: a real herdr-toast-install failure was printed and install.sh still exited 0 with "Done"; it now finishes its steps and exits 1 naming the toast step. Gate [medium], **confirmed and fixed**: the by-design skip matched any `refusing: ` line, which swallowed the installer's real-directory refusal; now matched on the two exact by-design texts. Second pass: approve. |
| agent-skills#1017 | #1072 | 2 | 0 | 0 | Two runs, two codex-only findings, one fixed. Gate [medium], **confirmed and fixed**: the regression test exited 0 where `unshare -rm` was refused, an absent run reading as a pass; ruled into a `NOTIFY_SEND_WSCRIPT` seam so the test always runs with no skip path. Second pass [medium], disputed: unchecked `mktemp`/stub setup — the witness is the timeout on the parse loop, which a missing stub does not affect. |
| agent-skills#1011 | #1073 | 1 | 0 | 0 | One run, approve, no material findings: four tidy-ups from #923's review (one canonical resolution statement, a shared timeout const, a test helper, a `--help` line); the resolver stays fail-closed. No second pass. |
| agent-skills#1025, #1027 | #1077 | 0 | 1 | 0 | One run, one [medium], also found by Claude: `tally_review_axes.py` reads the new `leftover` outcome as undisposed — the correctness axis's C1, already filed as #1076 (outside the diff, so not an adjacent fix). No second pass. |
| agent-skills#1026 | #1078 | 0 | 0 | 0 | One run, approve, no material findings: `loop.py dispatch` offers free slots widest closure first with a lowest-ticket tie-break, before the same-tick guard. No second pass. |
| agent-skills#1028 | #1080 | 2 | 0 | 0 | Two runs, two codex-only findings. Gate [high], **confirmed and fixed** (6d34ab0): the new in-round exception let the controller's read of a fix diff stand for every commit past the second pass's sha without binding it to a head sha, so a later push could merge unread; the controller now records the sha it read and `headRefOid` must equal it before merge. Second pass [high], **confirmed and filed** as #1082: the second-pass paragraph requires evaluating each finding where the ticket says each recommendation. |
| agent-skills#1075+#1079 | #1081 | 3 | 0 | 0 | Three runs, three codex-only highs, two fixed. Gate [high], **confirmed and fixed**: a resolved pane with a missing or unknown herdr status was neither working nor unlisted, so a listing of such panes read as an empty box. Second [high], **confirmed and fixed in-round** (first use of #1028's rule): the sessionId-to-pid join trusted a stale registry record, so a reused pid could be absorbed by an idle pane; `procStart` is now validated. Third [high], leftover: a foreign controller's working pane gets no fan-out reservation, pre-existing under the raw process count. |
| agent-skills#1029 | #1083 | 3 | 0 | 0 | Two runs, three codex-only findings. Gate [high], **confirmed and fixed** (b249fd5): `runfile.py leftover` skipped a sidecar line with a missing or unknown outcome, so a wrong `--from` copied zero and exited 0; now refused by file and line. Gate [medium], **confirmed and fixed** (b249fd5): leftovers could be recorded for an unlanded clump; now refused until `land`. Second pass [high], **confirmed and filed** as #1084: the sidecar is not bound to its PR, so another PR's valid sidecar is accepted. |
| agent-skills#1040 | #1088 | 1 | 0 | 0 | One run, one codex-only finding, disputed as unreachable: pre-fix worker records keep the raw path spelling and would miss a canonical key under a symlinked repo root, which no repo on this box has; also the worker's own P1 leftover restated. First disposition under the reachability bar (#1086). No second pass. |
| agent-skills#1044 | #1091 | 1 | 0 | 0 | One run, two codex-only mediums, both disputed. A failed `lock_shared()` falling back to the unlocked read: unreachable, `flock` on local ext4 under WSL does not fail, and the fail-closed rewrite is a new return type plus a second file, outside the adjacent-fix budget. The race test's 300 ms window: the worker's mutation check turned it red with the lock removed, and the technique is the file's own precedent. No second pass. |
| agent-skills#1042 | #1089 | 2 | 0 | 0 | Two runs, two codex-only mediums, both fixed. Gate: the fake's status route matched any `--json` field named `state` and tolerated a third fixture field; now exact `number,state` and two-field rows, loud otherwise. Second pass: no witness for a one-field row; a six-line should-panic test, fixed in-round as an adjacent fix under #1028's rule, no third run. |
| agent-skills#1030 | #1090 | 5 | 0 | 0 | Two runs, five codex-only findings. Gate [high], **confirmed and fixed** (629be79): the three closing-report counts were named with no source; `sweep.py counts` now reads each landed clump's dispositions sidecar and refuses a missing one. Gate [high], **confirmed and fixed** (c40793f): sweep filing could duplicate on a retry or the two-park stop path; the deterministic title is searched first. Gate [medium], **confirmed and fixed** (047b0d1): the rendered bullet omitted the clump. Second pass [high], **confirmed and filed** as #1093: `counts` resolves the review cache from the controller's cwd, which may not be the target repo. Second pass [medium], confirmed, **left over** (codex-second-M1): `counts` skips a malformed sidecar line and still exits 0. |
| agent-skills#1033 | #1094 | 3 | 0 | 0 | Two runs, three codex-only findings. Gate [high], **confirmed and fixed** (342d3c6): the fold appended the per-PR ticket's whole body, including its own `## Blocked by`, and `frontier.blocked_by_section` returns AMBIGUOUS on two declarations, so the folded sweep would never reach the frontier; the fold now cuts at that heading and a frontier_test case witnesses it. Gate [medium], **confirmed and fixed** (c0b8934): the per-PR sweep had no retry search. Second pass [high], **confirmed and filed** as #1095: that retry's `gh issue edit <n>` names the ticket, not the sweep it found. |
| agent-skills#1098 | #1099 | 3 | 1 | 0 | Three runs, four findings. Gate [high], **codex-only, confirmed and fixed** (7559882): adopt removed the record from the dead sidecar before landing it, so a crash between lost the worker from both files; now landing-first, one adoption lock, and the orphan scan skips a copy a live controller holds, witnessed by a real SIGABRT mid-adopt. Second pass [high], **codex-only, confirmed and fixed** (2278987, adjacent by controller ruling though the test sits in a second file): a retry after a landed-but-unprinted adopt took the already-controls path and never re-pointed the worker. Third pass (final) [high], **codex-only, confirmed**, leftover folded onto #1101: a partial destination write followed by a retry buries the record behind an unterminated line. Third pass [high], **also found by Claude**: rewrite truncates before writing; the worker raised it before the pass and the controller filed #1101. No Claude axis raised the three codex-only findings. |
| twitch-rules-scroller#345 | twitch-rules-scroller#375 | 1 | 3 | 0 | Three runs across two controllers (dispatcher scroller-burn died after the gate pass; skills-1b adopted the worker with controller-adopt, #1098). Gate [high], **codex-only, confirmed and fixed** (c05acbf): `parseE2eChromePids` matched the e2e profile with `includes`, so a `<profile>-old` Chrome passed the capture-owner guard; now a whole-argument, quoted-or-not, case-insensitive match. Gate [high], also found by Claude (C1, disputed in round 1 as a documented limit): a non-default E2E_USER_DATA_DIR sorted the e2e Chrome as the stream browser; fixed in c05acbf by refusing it before launch. Second pass [high], also found by Claude (C7/P3): the owner check is not atomic against OBS's fallback bind; filed as #380. Second pass [high], also found by Claude (C10, dispute overruled): suffix exe versus OBS_STREAM_EXE mismatch; fixed in 765b5c3. Third pass approved. |
| agent-skills#1049 | #1102 | 0 | 0 | 0 | One run, approve, no material findings. No second pass. |
| agent-skills#1047 | #1103 | 0 | 0 | 0 | One run, approve, no material findings. No second pass. |
| agent-skills#1000 | #1105 | 0 | 0 | 0 | One run, approve, no material findings. No second pass. |
| agent-skills#1097 | #1111 | 0 | 0 | 2 | One run, needs-attention, two codex-only mediums, both disputed by the controller. M1: a known-outcome sidecar line with no payload is counted by `counts`; unreachable as a silent wrong count, since `runfile.py leftover` refuses an incomplete leftover line at every landing, before counts runs at close. M2: `file` rendered unescaped in the sweep heading; unreachable, `file` is a repo path our reviewers write and no path here holds `@` or `<`. Sha unmoved, no second pass. |
| agent-skills#1045 | #1108 | 1 | 0 | 0 | One run, one codex-only finding. Gate [high], **confirmed and filed** as #1118: `tier.py --strip` reports that dispatch will strip `documentation` when a candidate-line path is code, but dispatch reads only the body's paths and nothing acts on the report, so a doc-labelled ticket whose code target is only on the candidate line still ships light. Crosses dispatch, tier.py and burndown/SKILL.md, so not an adjacent fix. Sha unmoved, no second pass. |
| agent-skills#1059 | #1119 | 0 | 0 | 0 | One run, approve, no material findings. The worker's own C1 (a report to a nameless twin's socket now reads silent) was filed as #1114 before the pass and named in the appendix; Codex did not raise it. No second pass. |
| agent-skills#1056 | #1116 | 1 | 0 | 0 | Two runs, one codex-only finding. Gate [medium], **confirmed and fixed** (75000f8, adjacent): the ticket asked for the block-boundary limit to be stated until #1115 lands, and § Known limits did not state it while the prose said "same paragraph". Second pass approved. |
| agent-skills#1087 | #1121 | 0 | 0 | 0 | One run, approve, no material findings. No second pass. |
| agent-skills#1085 | #1123 | 0 | 0 | 1 | One run, one codex-only [high], disputed by the controller as unreachable: a sidecar rewritten between `leftover()`'s read and its mtime check needs a concurrent writer, and the only writers (verification pass, then the controller, sequentially in one session) never overlap; Codex itself called it an inference. Sha unmoved, no second pass. |
| agent-skills#1107 | #1127 | 0 | 1 | 0 | Two runs. Gate [high], **also found by Claude** (S1/P1, left over in round 1), **fixed** (cc40c88): `--run` was optional, so omitting it still charged hand-copied `job` fields from `--in-flight`; now required, and the run file is the only job source. The controller rewrote S1/P1's sidecar lines to fixed. Second pass approved. |
| agent-skills#1096 | #1129 | 0 | 1 | 1 | Two runs, one finding raised twice. Gate [high], **also found by Claude** (spec-level review C7, left over): the sweep update path rewrites the body without `## Blocked by`, so an updated sweep reads unresolved; raised from leftover to **filed** as #1130 by the controller, close-out doc updated (c312220). Second pass [high] repeated it, objecting to closing the spec on a filed follow-up; **disputed** by the controller (already filed, fails closed). No third pass. |
| agent-skills#1076 | #1131 | 0 | 0 | 1 | One run, one codex-only [medium], disputed by the controller: the research tally counts a `leftover` line missing file/title/severity, but a count needs only the outcome, and `runfile.py leftover` refuses that line at every landing, so no malformed leftover reaches a sweep. Sha unmoved, no second pass. |
| agent-skills#1054 | #1133 | 0 | 0 | 0 | One run, approve, no material findings. No second pass. |
| agent-skills#1086 | #1135 | 1 | 0 | 1 | Two runs, two codex-only highs. Gate [high], **confirmed and fixed** (6ee21fd): the verification brief's fail conditions did not cover a `disputed: unreachable` claim, so the new bar could suppress a reachable finding unchecked; now a fourth fail condition, pinned. Second pass [high], **disputed** by the controller: a reachable third-pass high becomes leftover, not filed — pre-existing #1028 rule outside this ticket, and a leftover lands in the sweep. No third pass. |
| agent-skills#1084 | #1136 | 0 | 0 | 0 | One run, approve, no material findings. No second pass. |
| agent-skills#1093 | #1138 | 0 | 0 | 1 | One run, one codex-only [high], disputed by the controller as unreachable: `counts --repo` is not bound to the run's identity, but `--repo` is the controller's explicit input like the run id beside it, and no two repos on this box share a cache key; binding repo identity to the run file is a schema change outside the ticket. Sha unmoved, no second pass. |
| agent-skills#1052 | #1140 | 0 | 0 | 1 | One run, one codex-only [high], disputed by the controller as unreachable: the pr-body cache is keyed by repo basename, so two same-named repos on the same issue number could collide; no two repos on this box share a basename, and the key is the review cache's existing convention. Sha unmoved, no second pass. |
| agent-skills#1139 | #1141 | 0 | 0 | 0 | One run, approve, no material findings. No second pass. |
| agent-skills#1082 | #1143 | 0 | 0 | 0 | One run, approve, no material findings. No second pass. |
| agent-skills#1092 | #1142 | 0 | 0 | 0 | One run, approve, no material findings, at 04d03ab (after the controller ruled the ticket's `git ls-tree HEAD` half dropped: the flake was SIGPIPE under pipefail, not the index). No second pass. |
| agent-skills#1095 | #1150 | 0 | 0 | 0 | One run, approve, no material findings. No second pass. |
| agent-skills#1113 | #1153 | 0 | 1 | 0 | One run, one [medium] also found by Claude (C1/P1): `check_adjacent.py` still breaches a fix plus its own test file; filed as #1152 by the worker, not folded in by the controller since #1152 carries an open design decision. Sha unmoved, no second pass. |
| agent-skills#1114 | #1155 | 0 | 0 | 0 | One run, approve, no material findings. No second pass. |
| agent-skills#1151 | #1156 | 0 | 0 | 0 | One run, approve, no material findings (sweep PR fenced by the controller to files no live clump held). No second pass. |
| agent-skills#1118 | #1157 | 0 | 0 | 1 | One run, one codex-only [high], disputed by the controller as unreachable within the lane: a `documentation` label re-added between the tier pass and dispatch would still dispatch light, but nothing in the lane adds it after the pass and a human relabel is a ruling; the ticket offered (a) label-strip or (b) targets-into-dispatch and the PR took (a). Sha unmoved, no second pass. |
| agent-skills#1144 | #1158 | 1 | 0 | 1 | Two runs. Gate [high], **codex-only, confirmed and fixed** (bb9962f): `tests/all.sh` compared the identity only on the suite-success branch, so a suite that leaked a fixture identity and then failed — the likely shape of the real leak — went unnamed; its "restore the snapshot" half disputed by the controller (would write the real checkout's config). Second pass [medium], disputed by the controller as unreachable: the `<unset>` absent-key sentinel collides only with a literal `<unset>` identity, which nothing in the tree uses. No third pass. |
| agent-skills#1104 | #1162 | 0 | 0 | 0 | One run, approve, no material findings. No second pass. |
| agent-skills#1106 | #1160 | 1 | 1 | 0 | Three runs. Gate [high], also found by Claude (spec P1, which the worker had disputed; controller overruled): unlisted extensions on slash paths (`src/main.dart`, `tools/Gemfile`) read as prose, so a documentation-labelled ticket could dispatch light; fixed 80b3140. Second pass [high], **codex-only, confirmed and fixed** (f71cf86): a dotfile on a slash path (`config/.env`) had an empty stem and skipped the new rule. Third run (final): [high] bare unlisted filenames (`build.gradle`) and [medium] backslash paths, both leftover by the controller — the first is a design call against the ticket's own prose-token constraint. |
| agent-skills#1109 | #1163 | 0 | 0 | 0 | One run, approve, no material findings. No second pass. |
| agent-skills#1149 | #1164 | 0 | 0 | 1 | One run, one codex-only [high], disputed by the controller: stale local remote-tracking refs could exclude a foreign commit no longer on the remote, but the ticket prescribed `--not --remotes` (the new-branch arm already used it), the guard targets locally authored identity mistakes that no remote ref reaches, and `ls-remote` would put a network call in every push. Sha unmoved, no second pass. |
| agent-skills#1115 | #1167 | 0 | 0 | 0 | One run, one codex-only [medium], leftover by the controller: a `--- |` delimiter under pipe prose is read as a GFM table without matching the header cell count, so a valid multiline span below it reports a false cut (CI false positive on unusual prose only). Sha unmoved, no second pass. |
