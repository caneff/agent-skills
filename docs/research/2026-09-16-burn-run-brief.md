You are the Controller of a burn: /burndown by hand, before the skill is written. Terms (Run, Slot, Clump, Worker, Controller, Wake, Workspace) are in ~/.agents/skills/CONTEXT.md; read it first. Read ~/.agents/skills/implement/SKILL.md § Dispatch, § Control, § The PR and § The merge so you know what a worker does and sends and what you do at merge. Merging follows the hard rules in ~/.claude/CLAUDE.md and § The merge as they read when you act, not any restatement here; re-read them before every merge. Record what hurts as you go: every rough edge is a comment on https://github.com/caneff/agent-skills/issues/781 starting "Burn run, hurt:" (or "non-hurt:"), one per comment, as it happens.

Target: the open ready-for-agent queue of caneff/sudokumaker-custom-constraints, lowest numbers first, capped at 15 tickets. Primary checkout: /home/caneff/src/sudokumaker-custom-constraints. Slots: 3 (the box already carries 10 herdr agents and ~24 claude/node/python processes against Chris's 28-process cap). Run id: burn-2026-09-16-0905.

0. Where you are. Run `git -C /home/caneff/src/sudokumaker-custom-constraints rev-parse --show-toplevel` and `git branch --show-current` in your own cwd. Your cwd must be that primary checkout on its default branch, not a worktree under .claude/worktrees. If not, stop and tell Chris before anything else.
   Append `run burn-2026-09-16-0905 start` to ~/.cache/burndown/sudokumaker-custom-constraints.progress and to ~/.cache/burndown/sudokumaker-custom-constraints.cost. Every later reader of those files reads from the last `run` line down.
   Note: another worker is already live in this repo on #482, and there are unrelated worktrees under .claude/worktrees. Leave them alone; the frontier rule (unassigned, ready-for-agent) already excludes them.

1. Exploration pass, once, before any dispatch. Run one in-process Explore subagent (Agent tool, subagent_type Explore, model sonnet). Its brief: read every ticket in the capped queue end to end plus every "Settled:" comment on them; for each ticket list the files it would touch (by name), its size tag (docs-only, one-file, multi-file), its collisions (a file two tickets both touch) and the repo's end-to-end test seam that covers it and what that seam cannot see (from AGENTS.md or the test tree); check every settled decision that asserts runtime behaviour against the code, quoting the implementing line anchored on a name, and sort each mismatch into exactly one of: NOT-YET-BUILT (the code lacks it and a ticket in this queue builds it; name that ticket) or CONTRADICTED (the code does something different from what the decision claims exists); list the questions a builder would otherwise stop to ask (acceptance the ticket leaves ambiguous, behaviour a player or user needs that it is silent on, a design choice it leaves open), each with a recommended answer and that answer's cost; write it all to ~/.cache/burndown/sudokumaker-custom-constraints.burn-2026-09-16-0905.notes.md with one section per ticket headed exactly `## #<n>` and one headed exactly `## Collisions`; return a summary under 40 lines.
   - CONTRADICTED is a ruling for Chris: post it on the ticket, hold that ticket, wait for his answer. NOT-YET-BUILT is a note only.
   - A docs-only ticket without the `documentation` label: add the label before dispatch, and comment why, so dispatch picks the light tier.

1b. Ruling round, before any dispatch. Put every CONTRADICTED item and every listed builder question to Chris in your terminal: a few numbered questions per round, each with your pick and its cost, until none is open. Post each answer on its ticket as a `Settled:` comment, so the worker reads it in step 3's notes. Dispatch in step 3 only tickets with no open question; a ticket still waiting on an answer is held, not the whole burn. Count the rounds and questions for the tally.

2. Clump. Draw the collision graph over the queue and take its connected components. Each component is one Clump: one worker, one workspace, one PR closing every ticket in it. Write the clumps under `## Collisions`, each with its tickets in number order and the union of their files. Merge method is squash: per-ticket commit shas do not survive it, so nothing (a test fixture, a pinned REF_COMMIT, a floor) may pin a sha from the branch; pin a committed copy instead.

3. The loop. A clump is on the frontier when every ticket in it is open, ready-for-agent and unassigned, and no ticket in it has an open blocker outside the clump; blockers between tickets of the same clump are ignored. Frontier clumps go lowest ticket number first. While there is a frontier clump and a free slot:
   - Run `uptime` and `free -g`. If the 1-minute load is above 24 or free memory is under 6 GB, dispatch nothing and record it as a hurt.
   - `implement-dispatch --repo /home/caneff/src/sudokumaker-custom-constraints <lowest ticket in the clump>` (add `--model opus` when the notes tag it multi-file). Relay its report line. Record the worker's herdr agent name (stable across restarts) and its Claude session name (from the dispatch report, else ListAgents).
   - If the clump has more than one ticket, claim the rest yourself now: for each other ticket, `gh issue edit <n> --repo caneff/sudokumaker-custom-constraints --add-assignee @me --remove-label ready-for-agent --add-label in-progress`. Then SendMessage the worker's session name with, in one message: the other ticket numbers in number order and that they are already claimed; that blockers between them are internal to the clump and to be ignored; that each ticket's last commit carries its `Closes #<n>` (never squash, reorder or rewrite commits to get there); that the PR body lists every ticket as a bare `Closes #<n>` line, not in backticks; the clump's own files; the notes file path and the `## #<n>` headings to read; and that every question and the PR-up report go to you by SendMessage, never printed in its pane. Record whether that message was enough.
   - Append `burning #<n>` for each ticket in the clump to the progress file.
   Then end your turn. Your wait is going idle. Never sit in a long tool call, a sleep, a blocking wait or `herdr agent wait` while workers are out: their messages are held until the call returns.

4. On every wake:
   - A question: rule on it yourself under the Controller entry in CONTEXT.md; escalate to Chris in your terminal only a spec-ruling change, a new dependency, an irreversible deletion, or something a player or user needs that the ticket is silent on. A ruling on runtime behaviour is checked against what ships (the live editor, not the headless bundle or another proxy) before you give it. Before escalating, you may commission the worker to run one bounded probe (a stated cap on runs, time and tools) so Chris gets a table, not options in the abstract.
   - "PR up": run `gh pr view <pr> --repo caneff/sudokumaker-custom-constraints --json isDraft,mergeStateStatus,closingIssuesReferences` and check not-draft, CLEAN, and every clump ticket in closingIssuesReferences; if not, send it back. Then merge per § The merge and the hard rules, in this pane, never relayed to another session. Append `#<n> pr <url>` then `#<n> landed <sha>` for each ticket, run `python3 ~/.agents/skills/burndown/cost.py <workspace path>` and append `<n> <builder> <review> - 0` to the cost file (first ticket of the clump gets the numbers, the rest 0), then re-run step 3.
   - A "stopped without reporting" alert, or a worker you find done with no report: `herdr agent read <agent>` for what it printed, then treat it as the report it should have sent.
   - Before ending any turn with workers out, check each live worker with `herdr agent get <agent>`. One that is gone with no report is parked: comment on its ticket what was found, swap in-progress to ready-for-human, append `#<n> parked: <why>`, tell Chris. Two consecutive parks with no landing between stop the run: append `done`, report, end.

5. After a restart (WSL or harness): session names change. Run ListAgents, match each live worker to its herdr agent name, and SendMessage every live worker your new session name as its controller. Record the rename as a hurt only if a message was lost.

6. A ready ticket whose parent carries the `spec` label is not dispatched plain: dispatch `implement-dispatch --spec <parent> --slots <k>` with k taken from the burn's free slots; it counts against the cap as its number of open slices and reports back as one unit. Its closing ticket gets the list of the spec's merge shas from the progress file, not a git range.

7. When the queue is empty or the cap is reached and every PR has landed: append `done`, post a final comment on https://github.com/caneff/agent-skills/issues/781 with the tally (tickets landed, parked, dispatches, wakes, reports that never arrived, box checks that refused, Chris rulings before dispatch and after it, controller rulings reversed, and the hurt list in one place), and stop.

Rules that stand throughout: you never edit code in /home/caneff/src/sudokumaker-custom-constraints; merging and cleanup follow ~/.claude/CLAUDE.md and implement/SKILL.md § The merge as they read at the moment you act; every question a worker asks that times out is the worker's to answer safely, not yours to chase; a worker's report that arrives cut off is read from the file it names.
